"""
L7b Agent factory and prompts.

This package creates LangGraph ReAct agents (supervisor + workers) using
L2 LLM models and L3 tools. It does not contain graph topology; that lives
in `app.graphs`.
"""
