# 开发计划 v1.2

> 依据：`落地方案.md` v1.4 单机胖客户端 + harness 9 层分层架构 + LangGraph Supervisor/Workers 多 Agent
> 执行原则：严格遵照 @.opencode/karpathy-guidelines/SKILL.md
> 每个任务采用 TDD：**先写失败测试 → 写实现 → 测试通过 = 完成**
> 总工期：12 周（M5 末段含 1 周 PyInstaller 缓冲）

## 全局规则（每个任务必守）

1. **Think Before Coding**：任务开始前在该任务记录里复述假设；如发现文档没说死的细节，先问、不要选实现方案
2. **Simplicity First**：每个任务的实现代码量应等于「最小满足验收的代码」；任何未在验收中提到的字段/方法不写
3. **Surgical Changes**：只在任务声明的模块下新增文件；不动其他层、不重构未列出的部分
4. **分层契约**：每完成一层新增子包，立刻跑 `lint-imports`（import-linter）确认未违反 §4.6 分层规则
5. **TDD 强制**：每个任务验收的第一步是「红色测试存在」，再写实现

进度状态约定：`[ ] 未开始` / `[~] 进行中` / `[x] 完成`

---

## M1 — Python 后端 LangGraph 多 Agent 通跑（第 1-2 周）

> 验收：本机 `curl POST /api/chat` 发「在 ~/Desktop 写 hello.txt 内容 hi」，Supervisor 路由到 FileWorker，调用 `fs.write`，trace 写入 SQLite，文件落盘。开发模式（Python + Vite 分跑，不打 PyInstaller 包）。

### 1.1 项目骨架 + import-linter 分层契约 — [~] 进行中

- **需求目标**：后端工程空子包就位，import-linter 能跑过「空契约」，CI 基建可用。
- **实现步骤**：
  1. `uv init backend`，目录树如 §12
  2. `pyproject.toml` 添加 deps：`fastapi uvicorn pydantic-settings structlog pytest pytest-asyncio httpx freezegun import-linter`
  3. 建 11 个 harness 子包：`server`、`orchestrator`、`graphs`、`runtime`、`agents`、`memory`、`observability`、`guardrails`、`tools`、`llm`、`sandbox`，各含空 `__init__.py`
  4. 写 `backend/.importlinter`（§12.1 内容），harness 分层 layers contract + 横切层 forbidden contract
  5. 写 `tests/test_layering.py`，调 `subprocess.run(["lint-imports"])` 断言 exit 0
- **关键技术细节**：
  - `.importlinter` 用 `type = layers`，layers 列表按 §4.6 顺序：server → orchestrator → graphs → runtime → agents → memory → observability → guardrails → tools → llm → sandbox
  - L5/L4/L2/L1 横切：单独 `forbidden` contract 禁止它们 import server/orchestrator/graphs/runtime/agents
  - main.py 不入 contract
- **验证结果**：`uv run lint-imports` 4 contracts kept；`uv run pytest tests/test_layering.py -v` 1 passed

### 1.2 L2 模型层 gateway（LangChain 兼容）

- **需求目标**：提供模型无关的 `create_model(provider, model, api_key)`，返回 LangChain `BaseChatModel`，供 LangGraph Agent 使用。
- **实现步骤**：
  1. `app/llm/gateway.py`：`create_model(provider: str, model: str, api_key: str) -> BaseChatModel`
  2. `providers/anthropic.py`：包装 `langchain_anthropic.ChatAnthropic`
  3. `providers/openai_compat.py`：包装 `langchain_openai.ChatOpenAI`，通过 `base_url` 兼容 OpenRouter / 本地 vLLM / GLM
  4. `token_counter.py`：用 `tiktoken` 估算 messages token 数（用于 budget）
- **关键技术细节**：
  - 所有 provider 返回**同一 LangChain BaseChatModel**（关键：LangGraph 只认 LangChain 模型）
  - 测试 mock `ChatAnthropic` / `ChatOpenAI` 的 `ainvoke` / `astream`，不依赖真实网络
  - 不实现 `embed`（M5 才加）、fallback（M3 才加）

### 1.3 L2 router + model_picker

- **需求目标**：按用途选模型配置；token 计数。
- **实现步骤**：
  1. `router.py`：常量 dicts `PRIMARY={"supervisor": "claude-sonnet"}, COMPRESS={"compress": "gpt-4o-mini"}`
  2. `model_picker(task_kind) → tuple[provider, model_name]`
  3. `token_counter.py`：`count(messages) → int`
- **测试**：传 kind=supervisor 返回主模型；token 数已知文本断言 ±10%

### 1.4 L6 记忆层 short_term

- **需求目标**：每任务独立会话上下文存取，键为 task_id；供 graph state 初始化时注入。
- **实现步骤**：
  1. `short_term.py`：`class ShortTermStore: def __init__(self): self._ctx = {}`
  2. `get(task_id) / append(task_id, msg) / clear(task_id)`
  3. 不落盘（M5 才加 long_term），M1 用内存 dict 即可
- **测试**：两 task_id 隔离、append 累加、clear 不影响其他

### 1.5 L5 可观测层 trace + logger

- **需求目标**：事件总线 emit/subscribe；结构化日志 redact key；后续通过 LangGraph callback 消费事件。
- **实现步骤**：
  1. `trace.py`：`class TraceBus: def __init__(self): self._subs=[]`，`emit(event) / subscribe(cb) -> unsub`
  2. `logger.py`：structlog `configure_logger()`；自定义 processor `_redact(record)`，对 key 含 `api_key`/`authorization` 的字段值改 `***`
  3. SSE 路由（1.10）订阅 bus 推渲染进程
- **测试**：emit 一个 graph.step 后订阅者收到；含 `{"Authorization":"Bearer xxx"}` 的 log 输出字符串含 `***` 不含 `xxx`

### 1.6 L3 工具层 registry + fs（LangChain Tool 形态）

- **需求目标**：工具注册中心，暴露 LangChain `BaseTool` 实例；fs 内置工具实现读写列。
- **实现步骤**：
  1. `registry.py`：`class ToolRegistry: register(name, tool: BaseTool) / get(name) -> BaseTool / list_tools() -> list[BaseTool]`
  2. `fs.py`：用 `@tool` 装饰器定义 `read(path) / write(path, content) / list(path)`，write 内调 L1 `path_guard.check_path()`，越界抛 `PathGuardError`
  3. 工具 schema 由 LangChain 自动生成，无需手写 JSON Schema
- **测试**：注册 mock tool 后 `list_tools()` 含该 tool；fs.write 落盘后 read 回内容一致

### 1.7 L1 沙箱层 path_guard

- **需求目标**：路径白名单校验，resolve 后比对，防 `../` 穿越。
- **实现步骤**：
  1. `path_guard.py`：模块级 `set` 白名单；`check_path(p) → None|raise PathGuardError`
  2. 算法：`Path(p).expanduser().resolve()`，遍历白名单每项 `is_relative_to()` 任一命中放行
- **测试**：白名单 `["/tmp/a"]` 测 4 case（内/外/穿越/symlink 出去）

### 1.8 L7 Agent 工厂 + Supervisor/Workers Graph — 核心

- **需求目标**：实现 §5.1/§5.2 LangGraph 多 Agent：Supervisor 路由 + FileWorker 执行 `fs.write`，跑通「用户输入 → Supervisor → FileWorker → 工具调用 → 结束」完整 graph，写 trace 事件。
- **实现步骤**：
  1. `app/agents/factory.py`：
     ```python
     def create_supervisor(tools: list[BaseTool]) -> CompiledGraph
     def create_worker(name: str, system_prompt: str, tools: list[BaseTool]) -> CompiledGraph
     ```
     用 `langgraph.prebuilt.create_react_agent`
  2. `app/agents/prompts/`：supervisor.md / file_worker.md 最小 prompt
  3. `app/graphs/state.py`：`SupervisorState(TypedDict)` 含 `messages`, `next`, `task_id`
  4. `app/graphs/supervisor.py`：StateGraph，节点 supervisor / file_worker / web_worker / doc_worker，条件边路由，编译
  5. `app/runtime/graph_runner.py`：`async def run_graph(task, graph, bus) -> AsyncIterator[Event]`：调用 `graph.astream`，把 LangGraph event 转成 TraceBus event
  6. M1 阶段只实现 supervisor + file_worker；doc/web/msg_worker 留空接口
  7. M1 阶段不接 confirm_hub（工具调用静默直接执行）
- **关键技术细节**：
  - fake model fixture 用 `unittest.mock.AsyncMock` 让 `ChatModel.ainvoke` 返回固定 AIMessage(tool_calls=[...])
  - 3 fixture：supervisor 直接结束 / supervisor → file_worker 一次 tool call 后结束 / 51 步触发截断
  - 不调真实 fs.write（用 mock tool）

### 1.9 L7 runtime checkpointer + budget

- **需求目标**：graph 状态可中断恢复；成本闸生效。
- **实现步骤**：
  1. `runtime/checkpointer.py`：`AsyncSqliteSaver` 或 LangGraph `MemorySaver` 的本地 SQLite 包装
  2. `runtime/budget.py`：`Budget(max_steps, max_total_tokens)`；在 graph_runner 中每步累加 token/步数，超限时抛 `BudgetExhausted`
- **测试**：graph 执行到第 51 步抛 BudgetExhausted；checkpoint 能读取最近一次 state

### 1.10 L8 编排层 task_manager

- **需求目标**：把外部 TaskRequest 转成 LangGraph 调用并维护状态机。
- **实现步骤**：
  1. `task_manager.py`：`TaskManager(graph, tools, bus)`，`async submit(task_req) → task_id`、`status(task_id) → NEW|RUNNING|DONE|FAILED`
  2. 构造 `SupervisorState(messages=[HumanMessage(...)])`，调用 `runtime.run_graph`
  3. state 写内存 dict（M5 才落 SQLite）
- **测试**：submit 状态 NEW → task 跑完 DONE；fake model 报错时 FAILED

### 1.11 L9 入口层 chat route + main 装配

- **需求目标**：POST `/api/chat` 返回 SSE 流；FastAPI app 组合各层依赖注入。
- **实现步骤**：
  1. `main.py`：构造各层实例（tool registry 注入 path_guard，llm gateway 注入 token_counter，graph 注入 agents/tools），`create_app(task_manager, bus)` 返回 FastAPI
  2. `routes/chat.py`：`@router.post("/api/chat") async def chat(req): async def event_gen(): async for ev in task_manager.handle(req): yield f"data: {ev.json()}\n\n"; return StreamingResponse(event_gen, media_type="text/event-stream")`
  3. 事件 JSON 遵循 §5.6 协议
- **测试**：httpx + fake task_manager 注入固定事件流，断言 SSE 文本含 `data: {"type":"graph.step"...}`

### 1.12 M1 端到端冒烟

- **需求目标**：真起 uvicorn，端到端跑通 fs.write 任务，trace 写 SQLite（M5 才加持久化，本步先内存）
- **实现步骤**：`tests/e2e/test_m1_smoke.py` 起 `httpx.AsyncClient` 打真实 ASGI app，POST `/api/chat` 含 prompt「写 hello.txt」，fake model 让 Supervisor 路由 FileWorker 并调用 `fs.write`，断言文件真实出现在 /tmp 测试目录

---

## M2 — Electron 壳 + UI + 确认闸门(第 3-4 周)

> 验收：用户启动 Electron app → 输入「在 ~/Desktop 写 hello.txt」→ 弹确认 modal → 允许 → 文件落盘 → trace 实时显示。仍开发模式，不打包。

### 2.1 Electron 主进程 + python_runner

- **需求目标**：Electron 启动时 spawn 后端进程（dev 走 `uv run uvicorn`，打包走 PyInstaller exe），动态分配端口，健康轮询。
- **实现步骤**：
  1. `main.ts`：`app.whenReady()` → `python_runner.start()` → `createWindow()`
  2. `python_runner.ts`：`start()` spawn 子进程，传 `--port 0`，监听 stdout 找 `Listening on 127.0.0.1:<port>` 拿端口；轮询 `GET /health` 间隔 100ms，超 5s 报错
  3. main.ts 把 `http://127.0.0.1:<port>` 经 preload 注入 `window.api.backendUrl`
- **关键技术细节**：
  - dev 模式 `spawn("uv", ["run","uvicorn","app.main:app","--port","0"])`
  - packaged 模式 `spawn(path.join(process.resourcesPath,"python_bin"), [...])`
  - 测试 mock `child_process.spawn`，stdout 喂一行 `Listening on 127.0.0.1:54321`，断言拿到 port 54321

### 2.2 keychain.ts + preload

- **需求目标**：从 OS keychain 读写模型 key，经 IPC 暴露给渲染进程。
- **实现步骤**：
  1. `keychain.ts`：`getKey(service="my-cowork", account) / setKey(...)`
  2. `preload.ts`：`contextBridge.exposeInMainWorld("api", {getKey, setKey, backendUrl, ipcPrintPDF, ipcOpenPath})`
  3. main 在 spawn python 时 args 拼接 `--openai-key=<从keychain读>`（env 注入，不写配置文件）
- **测试**：mock `keytar.getPassword`，断言对象返回正确；env 注入字典断言含 key

### 2.3 渲染进程空骨架 + SSE client

- **需求目标**：React + zustand 空壳；SSE client 把 §5.4 事件转成 store action。
- **实现步骤**：
  1. `App.tsx` 一个 `<ChatView/>`
  2. `api/sse.ts`：`subscribeSSE(url, onEvent)` 包装 EventSource
  3. `store/session.ts` zustand：state `{messages, trace}`，actions `appendStep, appendDelta, appendToolResult`
- **测试**：mock `EventSource`，喂 `{type:"step.delta", payload:{delta:"hi"}}`，断言 store.messages 末尾含 "hi"

### 2.4 chat + trace UI

- **需求目标**：对话栏流式渲染；React Flow 把 trace 树形展示。
- **实现步骤**：
  1. `components/chat/ChatBar.tsx` 接受输入，POST `/api/chat`，转 SSE
  2. `components/trace/TraceTree.tsx` 用 React Flow，每个 step 一节点，边 = step→tool_result
  3. 事件流按 `step.start` 开新节点，`step.delta` 更新文本，`tool.result` 加子节点
- **测试**：喂录像 fixture（5 事件的 JSON 数组），断言渲染出 2 节点（1 step + 1 tool）

### 2.5 L4 护栏层 approval + command_filter — 核心

- **需求目标**：异步审批门+高危命令拒单，构成 L7→L4 的钩子。
- **实现步骤**：
  1. `approval.py`：`class ConfirmHub: def __init__(self, bus): self._futures={}`
     - `async def request(call_id, tool, args) → bool`：emit `tool.confirm_request`，`await self._futures[call_id]`，最多 `asyncio.wait_for(..., 600)`
     - `resolve(call_id, ok)` 唤醒 future
  2. `command_filter.py`：正则列表 `[r"rm\s+-rf\s+/(?!tmp)", r"chmod\s+-R\s+777\s+/", r"dd\s+if=/dev/zero", r"mkfs.*"]`；`check(cmd) → None|raise CommandForbidden`
- **关键技术细节**：
  - L3 `exec.py` 调用顺序：先 `command_filter.check`（命中直接抛），未命中再 `confirm_hub.request`（弹窗），获 ok 后真执行
  - ConfirmHub 必须挂在 task_manager 单例（多 task 共享 future 池）
  - 测试用 `freezegun` + 真实 asyncio loop，超时场景 `wait_for` 改 0.1s 模拟 600s

### 2.6 L4 护栏层 policy

- **需求目标**：Skill `allowed_tools` 白名单校验。
- **实现步骤**：
  1. `policy.py`：`check_tool_allowed(skill_meta, tool_name) → bool`
  2. Skill 加载后 meta 含 `allowed_tools: list[str]`，支持 `*` 通配（如 `mcp.github.*`）
- **测试**：声明 `[builtin.fs.write]` 调 `lark.send` 拒；声明 `[mcp.github.*]` 调 `mcp.github.get_pr` 放行

### 2.7 确认弹窗 UI

- **需求目标**：订阅 SSE `tool.confirm_request` 弹 modal，按钮 POST `/api/tool/confirm`。
- **实现步骤**：
  1. `confirm_modal.tsx`：组件挂载时订阅 store 的 `confirmQueue`，FIFO 弹出顶部
  2. rendering 命令/路径/参数（exec → 展示 cmd；fs → 展示 path+content 摘要）
  3. 点击「允许」→ `fetch("/api/tool/confirm/"+id, {method:"POST", body:JSON.stringify({ok:true})})`；「拒绝」→ `{ok:false}`
- **测试**：props 注入 fake confirmRequest，断言两种点击分别 POST 正确 body（mock fetch）

### 2.8 L9 confirm route

- **实现步骤**：`routes/confirm.py` `@router.post("/api/tool/confirm/{call_id}") async def confirm(call_id, body): confirm_hub.resolve(call_id, body.ok)`
- **测试**：注入 fake confirm_hub，POST 后 `resolve.assert_called_once_with(id, True)`

### 2.9 L3 工具层 exec

- **需求目标**：执行 LLM 生成 shell，串通 command_filter + confirm + path_guard。
- **实现步骤**：
  1. `exec.py`：`async def bash(cmd, cwd) → str`
  2. 调用序：`path_guard.check_path(cwd)` → `command_filter.check(cmd)` → `confirm_hub.request(...)` → 真执行 `subprocess.run`
  3. 返回 stdout/stderr/exit_code
- **测试**：mock subprocess；测 4 case：cwd 越界拒、命令高危拒、用户拒、用户允→成功

### 2.10 L1 net_guard + sandbox policy

- **需求目标**：域名白名单 + L0/L1 级别策略。
- **实现步骤**：
  1. `net_guard.py`：`check_domain(url) → None|raise NetForbidden`；从 `allowed_domains` set 比对 `urllib.parse.urlparse(url).netloc`
  2. `policy.py`：`class Policy: def __init__(self, level, skill_req_confirm)`；`requires_confirm_for(tool) → bool`：默认 fs/exec 走 confirm；Skill 声明 `requires_confirm: false` 时仅对低危（`os.open_path`）静默，fs/exec 仍走 confirm
- **测试**：net 白名单 `["api.openai.com"]` 测放行/拒；policy 测 `os.open_path` 静默、`fs.write` 即便 Skill 标 quiet 仍 confirm

### 2.11 设置页 UI

- **实现步骤**：
  1. `settings/Settings.tsx` 三个 tab
  2. 白名单 tab：数组编辑 + 保存（POST `/api/admin/whitelist`）
  3. key tab：调 `window.api.setKey("openai", v)`
- **测试**：点击保存→fetch 被调用 + zustand state 更新

### 2.12 M2 端到端冒烟

- **需求目标**：Electron 起窗口→输入→弹窗→落盘
- **实现步骤**：`e2e/m2.spec.ts`（Playwright Electron），fake LLM 返回 fs.write call，断言 modal 弹起 → 点允许 → ~/Desktop/hello.txt 真存在

---

## M3 — 文档生成 + MCP（第 5-6 周）✅

> 验收：输入「生成 PPT《2026Q1》5 页」→ 文件落桌面；「打开 example.com 截图」→ 截图落盘。

### 3.1 docx_gen ✅

- **需求目标**：LLM 产大纲 JSON，python-docx 渲染 docx 落盘。
- **实现步骤**：
  1. 定义大纲 schema（pydantic）：`{"title": str, "paragraphs": [{"heading": str, "body": str}]}`
  2. `docx_gen.py` `async def gen(outline, out_path) → path`：`Document()` → add_heading → add_paragraph → save
  3. tool schema：参数 `outline: dict, out_path: str`
- **测试**：注入 3 段大纲，断言 docx 经 `python-docx` 重新打开有 1 个 title + 3 段

### 3.2 pptx_gen 模板驱动 ✅

- **需求目标**：3 套 pptx 模板，LLM 选模板+填字段。
- **实现步骤**：
  1. 模板 `skills/_templates/tpl-{minimal,business,creative}.pptx`（手工预制）
  2. `pptx_gen.py` `gen(template_id, slides: list[{"title","bullets"}], out_path) → path`：`Presentation(template_path)`，遍历 slides 填占位符
  3. tool schema：暴露 `template_id` enum 3 值
- **测试**：3 模板各跑一遍，断言每张 slide 标题/bullets 与输入对

### 3.3 xlsx_gen ✅

- **需求目标**：openpyxl 填表。
- **实现步骤**：
  1. schema：`{"headers": list[str], "rows": list[list], "sheet_name": str}`
  2. `xlsx_gen.py`：建 sheet，先写 header，再 `append` rows
- **测试**：3 列 5 行 → 重打开 sheet dim 5×3（含 header 6×3）

### 3.4 pdf_gen + Electron IPC printToPDF — 跨层 ✅

- **需求目标**：Python 准备 HTML → 调 Electron 主进程 printToPDF → 返回 bytes 落盘。绕开 weasyprint。
- **实现步骤**：
  1. `pdf_gen.py` `async def gen(html: str, out_path) → path`：
     - POST `http://127.0.0.1:<electron_ipc_port>/print-to-pdf` body `{"html": html}`，拿 PDF bytes，写盘
  2. `electron/ipc_handlers.ts` 启 Node http server on 随机端口，端点 `/print-to-pdf`：
     - `BrowserWindow` 隐藏 `loadURL("data:text/html;base64,"+btoa(html))` → `webContents.printToPDF({})` 返回 Buffer
  3. IPC 端口在 python_runner 启动时由 main 告知 python（环境变量 `ELECTRON_PDF_PORT`）
- **关键技术细节**：
  - 用 hidden `BrowserWindow` 而非 webContents offscreen（API 稳定）
  - 测试：python 侧 respx mock PDF IPC 返回 `%PDF-1.4...`，断言 bytes 写入；Electron 侧 supertest mock 一个 html，断言回调被调用
- **Karpathy 提醒**：不引入 weasyprint。若 IPC 实在打不通，启用风险预案（5.4 缓冲周内 fallback weasyprint）

### 3.5 mcp manager ✅

- **需求目标**：MCP stdio client，按 `config.toml` 启 server 子进程，把其工具注册进本工具 registry。
- **实现步骤**：
  1. `mcp/manager.py` `class McpManager`：
     - 读 `config.toml` `[mcp.servers]` 段（每条 `command + args`）
     - 每条 `subprocess.Popen`，按 MCP stdio 协议（JSON-RPC）发 `initialize` → `tools/list`
     - 把每个 MCP tool 包成 `ToolRegistry.register("mcp.<server>.<tool>", wrapper, schema)`
  2. wrapper：调 `tools/call` JSON-RPC，返回 result content
- **测试**：写一个 echo MCP server（stdin/stdout JSON-RPC echo 工具）Python 小脚本作 fixture，启动 McpManager 连它，断言 `registry.schemas()` 含 `mcp.test.echo`、invoke 返回 echo 结果

### 3.6 playwright MCP 示例 ✅

- **实现步骤**：`config.template.toml` 加段：
  ```toml
  [mcp.servers.playwright]
  command = "npx"
  args = ["@playwright/mcp"]
  ```
- **烟雾测试**：手工启一次后端，让 Agent 跑「用 mcp.playwright.navigate 打开 example.com + screenshot 存 ~/Desktop/shot.png」
- **验收**：截图真实落盘

### 3.7 filesystem MCP 示例 ✅

- 同 3.6，接入 `@modelcontextprotocol/server-filesystem`

### 3.8 M3 端到端冒烟 ✅

- **需求目标**：4 文档 + 截图端到端 4 件产出
- **实现步骤**：4 个独立 e2e 测试

---

## M4 — 飞书远程 + 调度（第 7-8 周）✅

> 验收：客户端启用飞书远程→飞书发消息生成 docx→弹窗→回飞书；cron skill 每周一 9 点触发。

### 4.1 tunnel.ts + cloudflared ✅

- **需求目标**：起 cloudflared 把 `127.0.0.1:<port>/webhook/*` 映射到公网 HTTPS URL；后端非 /webhook 路径仍 127 only。
- **实现步骤**：
  1. `tunnel.ts` `startTunnel(targetUrl) → Promise<string>`：spawn `cloudflared tunnel --url <target>`，监听 stdout 正则 `\$\$Running tunnel (https://[^\s]+)` 拿 URL
  2. 后端 `routes/__init__.py` 加一个 middleware：非 `/webhook/*` 且请求非 127.0.0.1 直接 403（防 tunnel 转发后绕过）
- **测试**：mock child_process，喂 stdout 含 URL 行；middleware 测：非 webhook + 远端 IP → 403；webhook + 远端 → 放行路由

### 4.2 L3 lark send_message ✅

- **实现步骤**：`lark/send_message.py` `async def send(chat_id, text) → msg_id`
- 用 `lark-oapi` SDK `im.v1.message.create_request`；从 env 取 `LARK_APP_ID/SECRET`
- **测试**：mock SDK，断言调用参数对

### 4.3 L9 webhook_lark ✅

- **需求目标**：飞书事件验签 + IP 白名单 + 转 TaskRequest。
- **实现步骤**：
  1. `webhook_lark.py` `@router.post("/webhook/lark")`
  2. IP 白名单：mid level `request.client.host in LARK_IPS` 否则 403
  3. 验签：`ENCARTA` 模式，HMAC-SHA256 `timestamp + "\n" + LARK_VERIFY_TOKEN` == `X-Lark-Signature`
  4. 解 message.content 转 `TaskRequest(text=..., source="lark", reply_chat_id=...)`；submit
  5. 任务完成时 hold 住响应异步 `lark.send_message(reply_chat_id, result_text)`（用 asyncio.create_task 不阻塞 webhook 响应）
- **测试**：正确签名通过、错误签名 401、白名单外 IP 403、白名单内生成 TaskRequest 进 task_manager

### 4.4 L9 scheduler ✅

- **需求目标**：APScheduler + 本机 SQLite jobstore，三种触发器，Skill 可声明 schedule 自动注册。
- **实现步骤**：
  1. `scheduler.py`：`AsyncIOScheduler(jobstore=SQLAlchemyJobStore("sqlite:///"+db_path))`
  2. `register_skill(skill_meta)`：若 skill.yaml 含 `schedule: "..."`，`add_job(run_skill, CronTrigger.from_crontab(...) 或 IntervalTrigger 或 DateTrigger)`
  3. `run_skill(skill_id, params)`：构造 TaskRequest source="cron" → submit
- **测试**：freezegun + 注册 cron `* * * * *`，fake_now 推进 61s 应触发一次 run_skill

### 4.5 webhook 触发时校验 local 工具依赖 — 关键约束 ✅

- **需求目标**：飞书触发时若 Skill 用 confirm 闸门类工具，任务直接拒并回飞书提示（v1.2 无 WS，无法弹远程确认）。
- **实现步骤**：
  1. webhook_lark submit 前查 Skill meta：若 `requires_confirm` 类工具在 allowed_tools 中且 source="lark" → 拒，回飞书「此技能需桌面客户端运行，请打开 app 后再尝试」
  2. policy.py 加 `def skill_usable_via_remote(skill_meta) → bool`（回到 L4 但被 L9 调用——这是横切，符合 §4.6 例外）
- **测试**：含 fs.write 的 Skill 经 lark 接收→返回 drone 拒；含 http.request 的 Skill 经 lark→放行

### 4.6 M4 端到端 + cron ✅

- **需求目标**：真建飞书自建应用，真触发一次任务 + 一个真 cron Skill 触发
- **实现步骤**：飞书自建应用、tunnel URL 配置，飞书发「用 weekly_report Skill 汇总成 docx 回我」，断言 5 分钟内飞书收到 docx；注册一个每分钟跑的测试 Skill，真等 1 分钟看触发
- **自动化**：`tests/e2e/test_m4_smoke.py`（mock Lark）；真机烟雾需本机 `cloudflared` + `LARK_*` env

---

## M5 — 记忆 + 成本闸 + 打包（第 9-12 周，含 1 周缓冲）✅

> 验收：记忆注入生效；超 200k token 硬截断；PyInstaller + electron-builder 打出 `.dmg`/`.exe`，干净机器装能跑通 M1 用例；写部署手册。

### 5.1 L6 long_term 向量记忆 ✅

- **需求目标**：sqlite-vec 持久化记忆，task 启动注入 top-3 相关记忆到 system prompt，用户说「记住 X」时写记忆。
- **实现步骤**：
  1. SQLite 表 `memory(id, task_id, kind, content, embedding BLOB, created_at, expires_at)` + `sqlite-vec` virtual table `vec_memory(rowid, embedding)`
  2. `long_term.py`：`write(content, kind) → rowid`（调 L2 `embed()` 生成 vector）；`query(text, k=3) → list`（vec_query topk）
  3. integration：`runtime/context.py` 启动时调 `long_term.query(task.description)` 把结果拼到 system prompt
  4. agent_loop 中识别「记住」「以后」关键字 → `long_term.write(...)`
- **关键技术细节**：
  - embedding provider 在 llm gateway 加一方法 `embed(text) → list[float]`（M1 没写，5.1 补）
  - sqlite-vec 用 `pip install sqlite-vec`, `conn.enable_load_extension(True); conn.load_extension("vec0")`
- **测试**：write 3 条 → query 关键词，断言返回 top-3 中包含相关条目（mock embed 固定向量）

### 5.2 L7 compressor ✅

- **需求目标**：token > 120k 触发，调廉价模型生成 summary，保留最近 5 步原始。
- **实现步骤**：
  1. `compressor.py` `async def maybe_compress(ctx) -> bool`
  2. 算 token 总数超 120k → 取 ctx.messages[:-5] 喂 L2 廉价 model（model_picker("compress")）→ 返回 summary text → 替换为 `[{"role":"system","content":summary}]` + 原 messages[-5:]
- **测试**：构造 130k tokens 的 ctx，跑 maybe_compress，断言新 ctx token count < 30k、末 5 step 原样保留；mock LLM 返回固定 summary

### 5.3 L7 budget 硬截断 + L5 metrics ✅

- **需求目标**：单任务 200k 硬截断；每日全局阈值告警事件。
- **实现步骤**：
  1. `budget.py` 增强：`consume_tokens(n)` 累加；超 max_total_tokens → `exhausted=True`
  2. agent_loop `while not exhausted` 检测后 yield `BudgetExhaustedEvent`
  3. `observability/metrics.py` `class MetricsStore`：表 `usage_log(task_id, tokens_in, tokens_out, usd, at)`；每日聚合查超阈值 → `bus.emit(metrics.daily_exceeded)`
- **测试**：130k 任务继续跑、200k 任务硬停；告警事件触发

### 5.4 PyInstaller macos.spec — 核心风险 ✅

- **需求目标**：把后端打成单一可执行，hiddenimports 全显式列出，mac 上产 .app/.bin 能在干净机器跑通 M1 用例。
- **实现步骤**：
  1. `build/pyinstaller/macos.spec`：
     - `Analysis(["app/main.py"], pathex=["backend"], hiddenimports=HIDDEN)` 其中 `HIDDEN = ["app.llm.providers.anthropic","app.llm.providers.openai_compat","app.tools.builtin.fs","app.tools.builtin.exec","app.tools.builtin.docgen.docx_gen","app.tools.builtin.docgen.pptx_gen","app.tools.builtin.docgen.xlsx_gen","app.tools.builtin.docgen.pdf_gen","app.tools.mcp.manager","app.memory.long_term","sqlite_vec","lark_oapi","structlog"]`
     - `EXE(..., name="my-cowork-backend", onefile=True, upx=True)`
  2. `scripts/build-python-macos.sh`：`uv sync && pyinstaller build/pyinstaller/macos.spec && dist/my-cowork-backend --port 0`，烟雾测试 grep `Listening on` 断言启动成功
  3. `.github/workflows/build.yml` 加 macos-latest job 跑此脚本
- **关键技术细节**：
  - `--onefile` 启动慢但分发简单；首次启动解压到 `~/.my-cowork/cache` 缓存
  - sqlite-vec 含 .dylib 原生扩展，需在 .spec `binaries` 显式收集：`collect_dynamic_libs("sqlite_vec")`
  - 若 hiddenimports 漏了某模块，运行时 `ImportError` 在 task 触发时才暴露 → CI 烟雾测试必须跑完整 M1 用例
- **缓冲起点**：若失败，启用缓冲周排查；若一周内无法解决 `--onefile`，降级 `--onedir`（接受 200MB 安装目录）
- **测试**：CI 跑过 + 本地手跑烟雾

### 5.5 PyInstaller windows.spec ✅

- **需求目标**：Windows 平台同样产出后端 .exe，CI 矩阵绿。
- **实现步骤**：
  1. 复制 macos.spec 调整：`binaries` 改 .dll；`pathex` 用 windows 分隔符
  2. `scripts/build-python-windows.ps1` 同 mac
  3. CI `build.yml` 加 windows-latest job
- **关键技术细节**：
  - sqlite-vec 在 windows 用 `vec0.dll`，`collect_dynamic_libs` 自动识别
  - 注意 lark-oapi 依赖某些 charset 库，PyInstaller `pyproject.toml` 可能要 `collect_all("lark_oapi")`
- **测试**：CI 通过；windows VM 手跑烟雾

### 5.6 electron-builder 配置 ✅

- **需求目标**：electron-builder 打 mac `.dmg` + windows `.exe`（NSIS 安装器），CI 自动出产。
- **实现步骤**：
  1. `build/electron-builder.yml`：
     ```yaml
     appId: com.my-cowork.app
     directories: {output: dist-electron}
     mac: {target: dmg, icon: build/icon.icns}
     win: {target: nsis, icon: build/icon.ico}
     nsis: {oneClick: false, allowToChangeInstallationDirectory: true}
     extraResources: [{from: dist/my-cowork-backend, to: python_bin}]
     ```
  2. `scripts/build-electron.sh`：`npm run build && electron-builder --config build/electron-builder.yml`
  3. CI 在 pyinstaller job 完成后跑此 job
- **关键技术细节**：
  - `extraResources` 把 pyinstaller 产物注入 app 内；`python_runner.ts` 在 packaged 模式从 `process.resourcesPath/python_bin` 读
  - mac 需 codesign（自用可临时 ad-hoc 签名 + `--no-quarantine` 提示用户）
- **测试**：CI 产出 `dist-electron/my-cowork-1.0.0.dmg` 和 `.exe`，工件大小 > 50MB

### 5.7 干净机器烟雾测试 ✅

- **需求目标**：在无 Python、无 Node、无 uv 的真实用户机器验证开箱即用。
- **实现步骤**：
  1. 在 macOS VM（Snapshot 还原点）+ Windows VM 各装 5.6 产物
  2. 启动 app → settings 填测试 openai key（造一个 testing key 走 mock provider）→ 输入「在 ~/Desktop 写 hello.txt 内容 hi」→ 弹窗 → 允许 → 断言文件真出现
  3. 记录启动时间，超 30s 标黄
- **关键技术细节**：
  - VM 用 UTM (mac) / Hyper-V (win) 快照回滚
  - macOS 装 .dmg 时遇 Gatekeeper 拦，记录用户提示步骤写进部署手册
- **验收**：两平台各跑通 M1 用例即通过；若失败回到 5.4 修复
- **说明**：流程写入 `docs/部署手册.md`；CI 冒烟覆盖后端启动，完整 VM 验收需人工

### 5.8 部署手册 + 开发指南 ✅

- **需求目标**：审阅者照手册独立跑通一台机器；开发指南含 import-linter 校验步骤。
- **实现步骤**：
  1. `docs/部署手册.md`：下载安装 → 首次启动 → 填 key → 安全提示 → 启飞书远程（可选）5 步图文
  2. `docs/开发指南.md`：本地 dev（uv + npm install + venv + vite + Electron 三终端启动）+ 写测试规范 + `lint-imports` 跑法 + 添加新 Skill 步骤 + 添加新 MCP server 步骤
- **验收**：团队内 1 人照文档独立跑通

### 5.9 缓冲周（第 12 周）

- 修复 5.4-5.7 中冒出的打包问题
- 文档补充
- 全部验收回归

---

## M6 — 可观测与可靠性（第二批完善）✅

> 验收：trace/audit 落库；侧栏 Trace 可见；停止真正取消 graph；checkpointer + ShortTerm/TaskStore 持久化；LLM fallback 单测绿。本批不做失败 replan。

### 6.1 TraceStore + TracePanel ✅
- `{data_dir}/trace.db`；TraceBus subscribe 落库；`GET /api/trace/{task_id}`；补发 `tool.result`
- SessionSidePanel「Trace」手风琴挂载 TracePanel（embedded）

### 6.2 AuditStore ✅
- `{data_dir}/audit.db`；ConfirmHub / CommandFilter / confirm 路由挂钩

### 6.3 Checkpointer ✅
- 生产 graph compile 注入 `AsyncSqliteSaver`（`{data_dir}/checkpoints.db`）；`astream` 带 `thread_id`
- 单测可用 `get_checkpointer(path, sync=True)`

### 6.4 真正 cancel ✅
- `TaskManager.cancel` / `cancel_all`；`POST /api/task/{id}/cancel`；`/api/chat/stop` 调 cancel

### 6.5 ShortTerm + TaskStore ✅
- ShortTerm SQLite（与 memory.db 同库表）；TaskStore `{data_dir}/tasks.db`

### 6.6 LLM fallback ✅
- `FallbackChatModel`；`MY_COWORK_FALLBACK=provider:model;...`

### 明确延后
- 失败 replan（重新 decompose）
- 完整 replay 播放器（仅落库 + GET）

---

## 附录：每周任务速查（按 Karpathy 第 4 条）

| 周 | 任务列表 | 周末验收（硬指标） |
|---|---|---|
| 1 | 1.1-1.5, 1.7 | 跑通 import-linter；L2/L5/L6/L1 单测全绿 |
| 2 | 1.6, 1.8-1.12 | curl POST 跑通 M1 端到端 |
| 3 | 2.1-2.4 | Electron 起窗口+python_runner+空 UI |
| 4 | 2.5-2.12 | 弹窗弹起 + 文件落盘端到端 |
| 5 | 3.1-3.4 | 4 类文档生成单元绿 |
| 6 | 3.5-3.8 | playwright/filesystem MCP 跑通 |
| 7 | 4.1-4.3 | cloudflared + lark + webhook 单测绿 |
| 8 | 4.4-4.6 | 真飞书发一次回执 + cron 触发 |
| 9 | 5.1-5.3 | 记忆注入 + budget 截断 |
| 10 | 5.4-5.6 | mac/windows 产物出现在 CI |
| 11 | 5.7-5.8 | 干净机器装跑通 + 文档完成 |
| 12 | 缓冲 | 全部验收回归 |

---

## 验证手段清单（每个周末必跑）

- [ ] `uv run pytest` 全绿
- [ ] `uv run lint-imports` 通过（分层契约）
- [ ] `npm run test`（前端单测）全绿
- [ ] `npm run test:e2e` 当周端到端通过
- [ ] 当周新增模块的 `lint-imports` contract 已收纳

---

## 风险触发条件（不停等事，提前动手）

- 1.8 LangGraph Supervisor/Workers 测试若发现 fake LLM fixture 不好构造 → 暂停 1 天，先做 `tests/conftest.py` 的 LangChain model recorder（不是新框架，pytest fixture）
- 3.4 printToPDF 跨进程 IPC 若阻塞 → 暂时降级走后端 weasyprint，5.4 一并处理（提前 1 周）
- 5.4 PyInstaller 若 hiddenimports 永远看不到底层 C 扩展 → 转 `--onedir` 而非 `--onefile`，接受 200MB 安装目录；LangGraph 的 checkpoint/sqlite 可能引入额外 hiddenimports，需在 spec 中显式列出
- 5.5 windows CI 若缺 cairo 依赖现已绕开（pdf 走 Electron），仅 python-docx/openpyxl 需验证，预期无问题

---

文档版本：v1.2 / 依据落地方案.md v1.4 / 严格遵照 karpathy-guidelines SKILL / TDD 强制 / 评审前先用