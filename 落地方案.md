# 智能办公协作 Agent 落地方案 v1.4

> 定位：团队内部自用的「通用办公自动化助手」—— 对标 Claude Cowork / WorkBuddy 类数字员工
> 形态：**单机胖客户端（Electron 内嵌 Python 后端）**，桌面应用为主体，Agent 直接操作用户本机
> 单租户、本地优先、可控成本、各成员独立部署互不共享
> 编写依据：OpenHands、OpenCowork、OpenWork、Eigent、CAMEL、deer-flow、water、omnigent、audit-harness 开源项目实测调研
> v1.4 变更：**Agent 内核从单 Agent 自研 Loop 改为 LangGraph + Supervisor/Workers 多 Agent 编排**。harness 9 层架构保留，L7/L8 调整为 LangGraph primitives（graphs/agents/runtime），不再自研 Loop。引入 `app.agents`、`app.graphs` 子包。
> v1.3 变更：**后端严格按 harness 工程架构 9 层分层重构**——编排层/执行层/工具层/护栏层/可观测层/沙箱层/记忆层/模型层/入口层，目录结构与模块边界严格对齐分层，代码自写不依赖具体 harness 框架
> v1.2 变更：由 v1.1 的 C/S 架构改为单机胖客户端——后端 Python 服务由 Electron 主进程 spawn 子进程跑在 localhost，飞书 webhook 经 Cloudflare Tunnel/Tailscale Funnel 暴露，定时调度本机跑；删除租用机、删除 WS 反向调用通道

---

## 1. 竞品调研结论（先有事实，再谈方案）

### 1.1 调研对象实测信息表

| 项目 | Star | 语言/运行时 | 沙箱方案 | Agent 框架 | 模型接入 | 远程触发 | 多智能体 | 文档生成 Skills | 桌面 GUI 操作 |
|---|---|---|---|---|---|---|---|---|---|
| **OpenHands** | 82.3k | Python(Uvicorn)+React | Docker / VM / 云后端可切换 | 自研 Agent Server + ACP 协议，可挂 Claude Code/Codex | 任意 LLM (LLM Profiles) | Slack/GitHub/Linear webhook + 定时 | 单 Agent 为主，Agent Canvas 编排 | 靠第三方 Agent | 靠第三方 |
| **Eigent** (eigent-ai/eigent) | 14.6k | 后端 FastAPI + 前端 React/Electron/TS | local-first，无强隔离 | **CAMEL-AI** 多智能体框架 | 云 API + Ollama/vLLM/LM Studio 本地 | 调度器 + webhook | **核心卖点：Workforce 并行分工** | 有(PPTX/DOCX/XLSX) | 有(Gemini Computer Use) |
| **OpenCowork** (OpenCoworkAI) | 1.9k | Electron + TS | **WSL2(Win)/Lima(macOS) VM 级隔离** | Claude Code 封装 + 自研 Skills 引擎 | Claude/OpenAI/GLM/Kimi/MML via OpenRouter + Anthropic base_url | **飞书(Lark)/Slack 远程控制** | 单 Agent | 有(PPTX/DOCX/XLSX/PDF) | 有(推荐 Gemini-3-Pro) |
| **OpenWork** (Xerus-ai) | 34 | Electron + TS + Vite | 工作区文件夹约束(弱) | **Claude Agent SDK** 直封装 | OpenRouter 统一路由 | 无 | 单 Agent | 有(PPTX/DOCX/XLSX/PDF) | 无 |
| **WorkBuddy(对标目标)** | — | 通用办公自动化数字员工 | — | — | — | — | — | — | — |

> 上述 Star/语言/沙箱/框架均来自各仓库 README 与 GitHub API 实测（2026-07-27）。
> 关键观察：**OpenWork 是单机胖客户端（Claude Agent SDK 跑在 Electron 进程内）已验证可行**；Eigent Local 模式虽是 C/S 但前后端都在本机 localhost；OpenCowork 是单机 + VM 隔离。
> 我们 v1.2 走 OpenWork 路线的「单机胖客户端」思路，但用 Python 后端替代 Claude Agent SDK 以保持模型无关。

### 1.2 关键设计取舍提炼（决定我们方案的依据）

1. **架构形态**：v1.0 用 Web UI + 本机后端，v1.1 用 C/S + 租用机，**v1.2 改为单机胖客户端（Electron 内嵌 Python 后端子进程）**。理由：(a) 用户明确要「桌面应用为主 + 直访本机磁盘」；(b) 用户「默认有公网」可用 Cloudflare Tunnel 暴露 webhook，无需租用机；(c) 各成员独立部署互不共享，无集中后端需求；(d) OpenWork 已验证单机胖客户端可行。代价：失去 24h 在线（关机不跑定时任务）、Python 打包进 Electron 工程量大（见 §4.4）。
2. **沙箱**：取消 VM/gVisor 强隔离。改为「Electron 进程内 + 用户授权弹窗 + 路径白名单 + 高危命令硬拒单」。理由：办公核心价值是直访本机文档，VM 隔离导致文件双向同步断裂；威胁模型靠每步 local 工具确认闸门兜底。
3. **Agent 内核**：**LangGraph + Supervisor/Workers 多 Agent 编排**。Supervisor Agent 负责理解用户意图并路由子任务到专业 Worker Agent（文档、邮件、搜索、文件系统等）。不引 CAMEL/CrewAI（抽象与 harness 分层冲突大）；不引 Claude Agent SDK（锁 Anthropic 协议，且 SDK 是 TypeScript，与我们 Python 后端不兼容）。
4. **多智能体**：采用 **Eigent 行为对齐的 LangGraph Workforce**（非 CAMEL）：TaskPlanner 拆解 → 用户确认子任务 → Coordinator 按依赖 `Send` 并行委派 Developer/Browser/Document/Multi-Modal → 共享笔记协作 → 失败 retry/replan（最多 3 次）。办公域工具与提示词仍按工人隔离。LangGraph 状态机支持审批分支与 human-in-the-loop。不引 CrewAI/CAMEL（抽象与 harness 分层冲突大）；不引 Claude Agent SDK。
5. **远程触发**：飞书 webhook 经 Cloudflare Tunnel（免费）或 Tailscale Funnel 暴露 Electron 内嵌后端的 localhost 端口。无租用机。定时调度用本机 APScheduler，app 关机则不跑（明确接受此约束）。
6. **模型 key 管理**：用户在客户端设置页填自己的 OpenAI/Anthropic/OpenRouter key，存本机 keychain（macOS Keychain / Windows Credential Manager，经 `keytar`）。Python 子进程启动时由 Electron 注入 env，key 不落盘配置文件。
7. **GUI 操作**：Computer Use 依赖强多模态模型，私有自用且省成本时作为范围外项。MVP 只做文件/文档/API/MCP 触达类任务。
8. **后端架构形态**：v1.4 严格采用 **harness 工程架构 9 层分层**（综合 deer-flow/water/omnigent/audit-harness 共性提炼）：编排层 / 执行层 / 工具层 / 护栏层 / 可观测层 / 沙箱层 / 记忆层 / 模型层 / 入口层。目录结构与模块边界严格对齐分层；L7 执行层引入 LangGraph 作为编排内核，其余层自写（不依赖 water-ai/deer-flow，避免框架锁定与抽象泄漏）。理由：harness 分层把「agent 周围基础设施」与「agent 本身」解耦，多 Agent 场景下仍保持各层职责清晰；water 框架定义最清晰但功能过重，「严格对齐分层原则」即可拿到 90% 工程收益。

---

## 2. 产品定义（边界先行，防止失控）

### 2.1 MVP 范围（必须做）

- 任务输入：**桌面客户端对话框** / 飞书机器人（经 Cloudflare Tunnel） / 本机定时调度三种入口
- 任务执行：LangGraph StateGraph 跑在 Electron 内嵌的 Python 子进程；Supervisor/Workers 调用工具全部本机进程内执行，无网络往返
- 任务产物：本机文件落盘 + 文本回报 + 可选推送飞书/Slack
- 可观测：实时 Trace 面板（推理 + 工具调用流式展示）、运行日志、token/成本计数
- 模型层：支持 OpenAI 兼容、Anthropic、OpenRouter 及本地（Ollama / LM Studio / vLLM）预设；OpenRouter/本地在存储上为 UX provider，注入后端时归一为 `openai_compat` + `BASE_URL`
- key 管理：用户各自填自己的 key，存本机 keychain；Models 面板 **Validate 通过后才保存**（对标 Eigent Agents→Models）

### 2.2 不做（明确排除，避免范围蔓延）

- 不做多租户/计费/SSO（团队内部自用，各成员独立部署）
- 不做云托管/SaaS 化
- 不做 Computer Use 桌面 GUI 操作（依赖 Gemini-3-Pro 级模型，成本与稳定性风险高）
- 不做自训练模型
- 不做复杂多智能体协商/市场/竞价编排（采用 LangGraph 实现 Eigent Workforce **行为**——拆解/依赖并行/笔记/失败恢复；不引入 CrewAI 角色扮演或 CAMEL Workforce 运行时）
- 不做集中共享后端（各成员机器独立，任务/记忆/Skills 默认不共享；Skills 可通过 Git 仓库手动同步，但非 MVP 内建功能）
- 不做无人值守 24h 在线（关机不跑定时任务，明确接受）

---

## 3. 总体架构

> 关键特征：**单机胖客户端**。Electron 主进程启动时 spawn 一个 Python 子进程跑 FastAPI on `127.0.0.1:<random_port>`；React 渲染进程 + Electron 主进程都通过 localhost HTTP/SSE 与 Python 后端通信；所有工具调用都在本机进程内执行，零网络往返。飞书 webhook 经 Cloudflare Tunnel 反向暴露 localhost 端口。

```
┌──────────────────── 用户本机 (单一桌面应用进程组) ───────────────────┐
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │  Electron 渲染进程 (React + Vite + TS)                        │    │
│  │   ├─ 对话栏 / Trace 面板 (React Flow) / Skills 库              │    │
│  │   ├─ 设置页: 模型选择 / 目录白名单 / 飞书 tunnel / key 管理     │    │
│  │   ├─ 工具调用确认弹窗 (local tool 授权 modal)                  │    │
│  │   └─ 系统托盘: minimized 保持运行, webhook 入口常驻            │    │
│  │       │                                                       │    │
│  │       │ HTTP/SSE 到 localhost:<port>                          │    │
│  └───────┼───────────────────────────────────────────────────────┘    │
│          │                                                            │
│  ┌───────▼───────────────────────────────────────────────────────┐    │
│  │  Electron 主进程 (Node.js)                                    │    │
│  │   ├─ spawn Python 子进程 (PyInstaller 打包的独立可执行)        │    │
│  │   ├─ keytar 读 keychain → 注入 Python 子进程 env              │    │
│  │   ├─ webContents.printToPDF (PDF 生成走 Electron 自带 Chromium)│    │
│  │   ├─ os.open_path (Finder/Explorer reveal, Node 原生)          │    │
│  │   └─ 自动更新 (electron-updater)                               │    │
│  └───────┬───────────────────────────────────────────────────────┘    │
│          │ spawn + localhost HTTP                                    │
│  ┌───────▼───────────────────────────────────────────────────────┐    │
│  │  Python 子进程 (FastAPI + Uvicorn on 127.0.0.1:<random>)       │    │
│  │                                                                │    │
│  │   ┌─────────────┐  ┌──────────────┐  ┌─────────────────┐      │    │
│  │   │ TaskManager │→ │ AgentRunner  │→ │ Tool Registry   │      │    │
│  │   │ 任务/会话状态│  │ LangGraph    │  │ 全部本机执行     │      │    │
│  │   └─────────────┘  └──────┬───────┘  └────────┬────────┘      │    │
│  │                          │                   │                │    │
│  │   ┌──────────────────────┴───────────────────┴──────────┐     │    │
│  │   │ 工具层 (全部本机进程内执行)                           │     │    │
│  │   │  fs.read/fs.write/fs.list (路径白名单校验)            │     │    │
│  │   │  exec.bash / exec.powershell (高危拒单 + 弹窗确认)    │     │    │
│  │   │  http.request (出网白名单)                            │     │    │
│  │   │  docx.gen / pptx.gen / xlsx.gen (Python 库)           │     │    │
│  │   │  pdf.gen → 调 Electron 主进程 printToPDF (IPC)        │     │    │
│  │   │  MCP Servers: playwright/notion/github (stdio)        │     │    │
│  │   │  skills.run (扫描本机 skills/ 目录)                   │     │    │
│  │   │  lark.send_message (lark-oapi)                        │     │    │
│  │   └──────────────────────────────────────────────────────┘     │    │
│  │                                                                │    │
│  │   ┌──────────────────────────────────────────────────────┐     │    │
│  │   │ 模型层 (LLM Gateway)                                   │     │    │
│  │   │  OpenAI / Anthropic / OpenRouter 统一抽象 + fallback   │     │    │
│  │   │  读 env 中的 key (由 Electron 主进程注入)              │     │    │
│  │   └──────────────────────────────────────────────────────┘     │    │
│  │                                                                │    │
│  │   ┌──────────────────────────────────────────────────────┐     │    │
│  │   │ 入口路由                                                │     │    │
│  │   │  /api/chat (渲染进程 SSE)                               │     │    │
│  │   │  /webhook/lark (飞书, 经 Cloudflare Tunnel 暴露)        │     │    │
│  │   │  /webhook/slack                                          │     │    │
│  │   │  APScheduler (本机定时, app 关机不跑)                   │     │    │
│  │   └──────────────────────────────────────────────────────┘     │    │
│  │                                                                │    │
│  │   ┌──────────────────────────────────────────────────────┐     │    │
│  │   │ 持久化 (SQLite + sqlite-vec 同库, 本机文件)             │     │    │
│  │   │  tasks / messages / trace / memory / usage_log          │     │    │
│  │   └──────────────────────────────────────────────────────┘     │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │  Cloudflare Tunnel / Tailscale Funnel (可选, 用户启用)        │    │
│  │   把 127.0.0.1:<port> /webhook/lark 映射到公网 HTTPS URL      │    │
│  │   飞书开放平台填这个 URL 作回调                               │    │
│  └──────────────────────────────────────────────────────────────┘    │
└───────────────────────────────────────────────────────────────────────┘
```

### 3.1 进程间通信协议

| 通道 | 用途 | 协议 |
|---|---|---|
| 渲染进程 ↔ Python 子进程 | 对话流、trace 流、任务管理 | HTTP + SSE（localhost）|
| Electron 主进程 ↔ Python 子进程 | 启动/健康检查/IPC 工具调用 | HTTP（localhost）+ stdout/stderr pipe |
| 渲染进程 ↔ Electron 主进程 | 文件对话框、keychain 读写、托盘、PDF printToPDF | Electron IPC（contextBridge）|
| Python 子进程 → Electron 主进程 | 调 `pdf.gen` / `os.open_path` | HTTP POST 到主进程起的 IPC 端口 |

> 关键点：**没有 WebSocket**。v1.1 用 WS 是因为后端在远端需反向调客户端执行 local 工具；v1.2 工具全在本机 Python 子进程内执行，SSE 单向推 trace 即可。仅 PDF 生成和文件对话框走 Electron 主进程 IPC（因为要用 Electron 自带 Chromium 渲染 HTML 转 PDF，以及调用原生文件对话框）。

### 3.2 工具调用全部本机执行（无 local/remote 路由）

v1.2 删除 v1.1 的工具路由表。所有工具都在本机 Python 子进程内执行，除两个例外走 Electron IPC：
- `pdf.gen`：Python 准备 HTML → IPC 调 Electron `webContents.printToPDF` → 返回 PDF bytes → Python 落盘
- `os.open_path`：IPC 调 Electron `shell.showItemInFolder`

其他工具（fs/exec/http/docx/pptx/xlsx/MCP/skills/lark）全部 Python 进程内直接执行。

---

## 4. 核心模块拆分与技术选型（带理由与具体版本边界）

### 4.1 技术栈总表

| 模块 | 选型 | 依据 |
|---|---|---|
| 后端语言 | **Python 3.11+** | AI 生态最齐；办公文档生成库成熟；与 OpenHands/Eigent 同栈 |
| 后端框架 | **FastAPI + Uvicorn** | Eigent 同款；异步 + SSE 友好；类型强 |
| 包管理 | **uv** | Eigent 同款；锁文件可复现 |
| Python 打包 | **PyInstaller** + `electron-builder` 组合 | PyInstaller 把 Python+依赖打成单可执行；electron-builder 把可执行 + Electron 前端打成 .dmg/.exe。OpenCowork/Eigent Local 没用此路径，是本方案主要工程风险（见 §4.4） |
| Agent 框架 | **LangGraph + Supervisor/Workers** | 多 Agent 编排标准解；human-in-the-loop、checkpoint、条件分支原生支持 |
| 工具协议 | **MCP (Model Context Protocol)** | OpenHands/OpenCowork/Eigent 全部支持，事实标准；可复用社区 MCP server |
| 模型接入 | 自研 `llm_gateway`，兼容 OpenAI v1 + Anthropic Messages 两套 schema | OpenRouter 走 OpenAI schema，Anthropic 原生走自身 schema，统一抽象可热切 |
| 桌面壳 | **Electron 30+** | OpenWork/OpenCowork/Eigent 同栈；Node sidecar 跑本机工具；自带 Chromium 可做 printToPDF |
| 客户端 UI | **React 18 + Vite + TypeScript** | 三竞品同栈 |
| UI 组件 | **Tailwind CSS + Radix UI + Lucide** | Eigent 同栈 |
| 状态管理 | **Zustand** | Eigent 同款 |
| Trace 可视化 | **React Flow**（Trace 视图用树形） | Eigent 同款 |
| 渲染进程↔后端通道 | **HTTP + SSE**（localhost） | 同机无需 WS；SSE 单向推 trace 足够 |
| 沙箱 | **进程内 + 授权弹窗 + 路径白名单 + 高危命令拒单** | 不再用 VM/gVisor（见 §4.3） |
| key 管理 | **keytar** (Node) 读 keychain → 注入 Python env | 用户各自填 key；不落盘配置；macOS Keychain / Win Credential Manager |
| 向量库 | **sqlite-vec** | 单机轻量；SQLite 同库 |
| 关系库 | **SQLite**（本机文件 `~/Library/Application Support/my-cowork/db.sqlite`） | 单机够用 |
| 调度 | **APScheduler** + SQLite jobstore | 本机定时；app 关机不跑（明确约束） |
| 文档生成 | **python-docx / python-pptx / openpyxl** + **Electron printToPDF**（PDF） | 前 3 类纯 Python 无系统依赖；PDF 走 Electron Chromium，**省掉 weasyprint+cairo 整个依赖链**（v1.2 关键改进，见 §6.3） |
| 浏览器自动化 | **Playwright** | 官方 MCP server `@playwright/mcp` 直接复用 |
| 飞书集成 | **lark-oapi (官方 Python SDK) + 自建 webhook** | OpenCowork 路线 |
| 公网暴露 | **Cloudflare Tunnel**（推荐，免费）或 Tailscale Funnel | 无需公网 IP / 无需租用机；用户在客户端一键启动 |
| 日志/Trace | **structlog + 内置 trace 写 SQLite** | 流式 Trace 是核心 UX |
| 自动更新 | **electron-updater** + GitHub Releases | 自用团队可简化为手动更新 |

### 4.2 为什么不用 Claude Agent SDK（OpenWork 路线）与自研 Loop

OpenWork 用 Claude Agent SDK 三天能跑通 MVP，但本方案不用，理由：
1. **锁 Anthropic 协议**：tool_use schema 是 Anthropic 专有 block 结构，与 OpenAI `tools` 字段不互通；换 GLM/Kimi/DeepSeek 等国内模型节省成本需大改
2. **TypeScript 后端 vs Python 后端**：Claude Agent SDK 是 TS，但办公文档生成生态（python-docx/openpyxl/python-pptx）全在 Python；选 TS 后端意味着文档生成要找 JS 替代库（docx.js / exceljs / pptxgenjs），这些库能力比 Python 版弱、模板生态差
3. **MCP Python client 更成熟**：官方 MCP SDK 优先支持 Python，TS 版本滞后
4. **多 Agent 编排不应自研**：自研 Loop 在单 Agent 场景可控，但 Supervisor/Workers、条件分支、checkpoint、human-in-the-loop 等能力用 LangGraph 已是事实标准，重复自研成本高且难稳定

因此 v1.4 采用 **LangGraph** 作为编排与执行内核，harness 的 L7/L8 只保留「胶水层」（启动 graphs、管理 checkpoint、注入工具/模型/护栏），不重新实现 Loop。

### 4.3 为什么放弃 VM/gVisor 强隔离

v1.0 用 Lima/WSL2+gVisor 双层隔离，v1.2 改为进程内 + 弹窗授权。理由按重要性排序：
1. **业务形态**：办公核心价值是直访本机文档（整理 ~/Downloads、读 ~/Documents 合同、生成 PPT 落到 ~/Desktop）。VM 隔离后产物落在 VM 内，需手动拖回本机，体验断裂
2. **威胁模型重估**：办公场景 LLM 生成 shell 90% 是 `ls/cp/mv/mkdir/python script.py`，且每条过用户确认闸门。强隔离防的是「无人值守自动执行不可信代码」，本方案默认每步 local 工具都需确认，威胁模型不成立
3. **单机胖客户端无 VM 必要**：v1.2 整个 Agent 跑在用户自己机器上，用户对自己的机器有完全控制权，不是多租户共享环境
4. **成本**：Lima 占 4-8GB 磁盘 + 启动 30s；删掉后客户端启动即用

### 4.4 Python 打包进 Electron 的工程风险与对策（v1.2 主要风险）

这是 v1.2 选胖客户端的最大工程难点，OpenWork 避开此问题是因为它用 TS 后端，我们选 Python 后端必须面对：

| 难点 | 对策 |
|---|---|
| PyInstaller 包大（Python 解释器 + 依赖 ~80-150MB） | 接受；自用团队内网分发，包大可忍 |
| weasyprint 依赖 cairo/pango 系统库，跨平台打包深坑 | **v1.2 删除 weasyprint，PDF 走 Electron printToPDF**（见 §6.3），彻底绕开 |
| Playwright 浏览器二进制大（~300MB） | 不打进主包；首次使用时 MCP server 触发 `npx playwright install chromium` 按需下载 |
| PyInstaller hiddenimports 漏打导致运行时 ImportError | CI 矩阵跑 macOS/Windows 烟雾测试，启动后跑一个 fs.write 任务验证 |
| Electron 启动 spawn Python 子进程慢（首次解压） | 用 `--onefile` 模式，首次启动解压到缓存目录，后续启动快；启动 splash 屏遮盖 |
| 跨平台编译需 macOS + Windows 分别打包 | 用 GitHub Actions 矩阵 build（macos-latest + windows-latest），不交叉编译 |
| 升级时整个 app 重装 | 接受；自用团队不频繁升级；electron-updater 增量更新前端，Python 部分随版本重装 |

### 4.5 为什么用 Cloudflare Tunnel 而非租用机做 webhook 网关

飞书 webhook 需要公网 HTTPS 回调。三种方案对比：

| 方案 | 成本 | 复杂度 | 评价 |
|---|---|---|---|
| 租用机做网关 forward | ¥180/月 | 多一层 forward 协议 | v1.1 路线，违背「无租用机」需求 |
| 用户机器直接开公网端口 | 0 | 需公网 IP + 路由器端口映射 + SSL | 多数家宽无公网 IP，且暴露面大 |
| **Cloudflare Tunnel** | 0 | 装 `cloudflared` + 一条命令 | 推荐：无需公网 IP，自动 HTTPS，仅暴露 /webhook 路径，攻击面小 |

v1.2 选 Cloudflare Tunnel：用户在客户端设置页点「启用飞书远程」，客户端自动调 `cloudflared tunnel` 把 `127.0.0.1:<port>/webhook/lark` 映射到 `*.trycloudflare.com` 公网 URL，URL 自动写回飞书自建应用回调配置（或提示用户手动填一次）。Tailscale Funnel 作为备选（已用 Tailscale 的团队零成本启用）。

---

### 4.6 harness 工程架构 9 层分层详解（v1.3 核心）

> 「harness」原意马具——把 LLM 当「被驾驭的马」，harness 提供马周围的所有控制基础设施，而非马本身。本方案后端严格按 9 层分层组织，每层有明确单一职责、对上对下接口边界，跨层依赖只允许向下，禁止反向。分层提炼自 deer-flow（super agent harness）/water（production-ready agent harness framework）/omnigent（meta-harness）/audit-harness（审计执行保障）的共性。

| 层 | 职责 | 对上接口 | 对下依赖 | 本方案实现 |
|---|---|---|---|---|
| **L9 入口层** `server/` | HTTP/SSE 路由、webhook 接收、调度触发、协议适配 | 外部请求 | L8 编排、L4 护栏（鉴权） | FastAPI routes、APScheduler、lark/slack webhook |
| **L8 编排层** `orchestrator/` | 任务生命周期、会话状态、子任务分发；负责把 TaskRequest 交给 LangGraph StateGraph 执行并维护任务句柄 | TaskRequest → TaskHandle | L7 graphs、L5 可观测（发 trace）、L6 记忆（取/存） | TaskManager、TaskHandle、会话状态机 |
| **L7 执行层** `runtime/` + `graphs/` + `agents/` | LangGraph 编排与执行：`graphs/` 定义 Supervisor/Workers StateGraph；`agents/` 定义各专业 Agent（含 tools、llm、prompt）；`runtime/` 负责 graph 调用、checkpoint、预算闸、上下文压缩 | graph event 流 | L3 工具、L4 护栏（审批门）、L5 可观测、L6 记忆、L2 模型、L1 沙箱（执行约束） | graph_runner、checkpointer、budget、compressor、supervisor_agent、worker_agents |
| **L6 记忆层** `memory/` | 短期会话上下文 + 长期向量记忆；记忆注入 system prompt、correction 持久化 | memory.read/write | 无（纯存储） | short_term、long_term(sqlite-vec) |
| **L5 可观测层** `observability/` | trace 事件、metrics、结构化日志、replay；跨层贯穿，各层都向它发事件 | emit(event) | 无 | trace、metrics、logger、replay |
| **L4 护栏层** `guardrails/` | 策略引擎、审批门、危险命令拒单、审计日志；在工具调用前后强制介入 | check/audit hooks | 无 | policy、approval（confirm_hub）、command_filter、audit |
| **L3 工具层** `tools/` | 工具注册中心、内置工具、MCP 适配、Skills 加载；纯「执行函数 + schema」，不含业务逻辑 | tool.invoke | L1 沙箱（路径/网络校验）、L5 可观测 | registry、builtin、mcp、skills |
| **L2 模型层** `llm/` | LLM 网关、provider 适配、fallback 链、token 计数与成本；只做「messages in → stream out」 | complete/embed | 无 | gateway、providers、router |
| **L1 沙箱层** `sandbox/` | 执行约束策略、路径白名单、出网白名单；v1.2 为进程内+授权，但策略层独立存在以便升级 | policy checks | 无 | policy(L0/L1)、path_guard、net_guard |

#### 分层依赖规则（硬约束，CI 用 import-linter 校验）

1. **只允许向下依赖**：L9→L8→L7→L 最底层；禁止反向（如 L2 模型层不得调 L3 工具层）
2. **L5 可观测层** 例外：可被任意层调用（跨层横切），但自身不依赖业务层
3. **L4 护栏层** 例外：可被 L7/L3 调用（工具前后钩子），但自身不依赖业务层
4. **L1 沙箱层** 例外：被 L3 工具层调用做前置校验，不反向
5. 每层暴露 `__init__.py` 作对外接口，内部模块不跨层直接 import
6. CI 配置 `import-linter`（`pyproject.toml` 声明 contract），违反分层直接 build 失败

#### 为什么这样分层（对齐 water 的核心理念）

water 项目的核心定义：「提供 agent 周围的基础设施，不是 agent 本身」。本方案把「agent 本身」（L7 graphs/agents/runtime + L2 模型）和「agent 周围基础设施」（L8 编排/L4 护栏/L5 可观测/L1 沙箱）严格分目录。收益：
- 替换 L7 编排图（如从 Supervisor/Workers 改成 Parallel Specialists）不动其他层
- L4 护栏独立后，审计/审批门可被任意层调用，复用进 webhook 入口校验；LangGraph human-in-the-loop 节点直接调用 L4 confirm_hub
- L5 可观测独立后，trace 事件结构统一，通过 LangGraph callback 消费后写 SQLite，replay/debug 工具可独立演进
- L1 沙箱独立后，v1.4 进程内策略与未来 Docker/gVisor 升级只改 L1 实现，上层无感

---

## 5. LangGraph 多 Agent 设计（归 L7/L8）

### 5.1 架构：Supervisor + Workers

```
                         ┌─────────────────┐
   用户输入 ─────────────▶│  Supervisor     │ 理解意图、拆解子任务、路由
                         │  Agent (L7)     │
                         └────────┬────────┘
                                  │ 调用 handoff/tool
              ┌───────────────────┼───────────────────┐
              ▼                   ▼                   ▼
        ┌──────────┐        ┌──────────┐        ┌──────────┐
        │ DocWorker│        │FileWorker│        │Search/   │
        │          │        │          │        │WebWorker │
        └────┬─────┘        └────┬─────┘        └────┬─────┘
             │                   │                   │
             └───────────────────┼───────────────────┘
                                 ▼
                         ┌─────────────────┐
                         │  Human-in-loop  │ L4 confirm_hub
                         │  节点（可选）    │ 高危/需确认工具弹窗
                         └─────────────────┘
```

- **Supervisor Agent**：不直接操作工具，只负责「理解用户请求 → 选择 Worker → 把子任务描述交给 Worker」。prompt 里列出所有 Worker 及其能力范围。
- **Worker Agent**：每个 Worker 是 `langgraph.prebuilt.create_react_agent` 创建的 ReAct Agent，绑定该领域工具：
  - `DocWorker`：`docx.gen`、`pptx.gen`、`xlsx.gen`、`pdf.gen`
  - `FileWorker`：`fs.read/write/list`、`exec.bash`、`os.open_path`
  - `WebWorker`：`http.request`、MCP playwright、MCP filesystem
  - `MsgWorker`：`lark.send_message`
- **Human-in-the-loop 节点**：LangGraph 的 `interrupt` 节点，在 Worker 调用 `fs.write`/`exec.bash` 等 L1 级工具前触发，经 L4 `confirm_hub` 等待用户弹窗确认。

### 5.2 Graph 结构

```python
# app/graphs/supervisor.py
from langgraph.prebuilt import create_react_agent
from langgraph.graph import StateGraph, START, END
from langgraph.types import Command, interrupt
from app.agents import supervisor_agent, doc_worker, file_worker, web_worker, msg_worker

class SupervisorState(TypedDict):
    messages: list[AnyMessage]   # LangGraph 标准消息列表
    next: str | None             # 下一个 Worker 名
    task_id: str
    usage: dict                  # token 累计

builder = StateGraph(SupervisorState)
builder.add_node("supervisor", supervisor_agent)
builder.add_node("doc_worker", doc_worker)
builder.add_node("file_worker", file_worker)
builder.add_node("web_worker", web_worker)
builder.add_node("msg_worker", msg_worker)
builder.add_node("confirm", human_confirm_node)   # L4 钩子

builder.add_edge(START, "supervisor")
# supervisor 返回 Command(goto=...) 动态路由
builder.add_conditional_edges("supervisor", route_to_worker)
builder.add_conditional_edges("confirm", route_after_confirm)
# Worker 完成后回到 supervisor 做下一步决策
builder.add_edge("doc_worker", "supervisor")
builder.add_edge("file_worker", "supervisor")
builder.add_edge("web_worker", "supervisor")
builder.add_edge("msg_worker", "supervisor")

graph = builder.compile(checkpointer=sqlite_checkpointer)
```

### 5.3 Agent 工厂（app/agents/）

```python
# app/agents/factory.py
from langgraph.prebuilt import create_react_agent
from app.llm import create_model
from app.tools import tool_registry

def create_worker(name: str, system_prompt: str, allowed_tools: list[str]):
    model = create_model()  # L2 网关返回 LangChain BaseChatModel
    tools = [tool_registry.get(t) for t in allowed_tools]
    return create_react_agent(model, tools, state_modifier=system_prompt)

def create_supervisor(system_prompt: str):
    model = create_model()
    # supervisor 可以带一个 handoff 工具，让 LLM 显式选择 next worker
    return create_react_agent(model, [handoff_tool], state_modifier=system_prompt)
```

关键边界条件（仍然保持）：
1. `max_steps=50` 整个 graph 步数上限（含 supervisor + workers）
2. `max_total_tokens=200_000` 任务级成本闸
3. `per_tool_timeout=120s`
4. `max_confirm_wait=600s` human-in-the-loop 等待上限
5. 出站网络白名单在 `http.request` 工具层强制

### 5.4 预算与截断

`app/runtime/budget.py` 提供一个 `BudgetMiddleware`（或 LangGraph `RunnableConfig` 中的回调），在每次 LLM 调用后累加 token。超限时：
- 抛出 `BudgetExhausted` 异常
- LangGraph checkpointer 保存当前状态
- `app/orchestrator/task_manager.py` 捕获后 emit `task.error` 事件

### 5.5 上下文压缩策略

Worker 内部由 LangGraph 自动维护 messages。当 messages 总 token > 120k 时：
- `app/runtime/compressor.py` 介入，取 `messages[:-5]` 调廉价模型生成 summary
- 替换为 `[{"type":"system","content":summary}] + messages[-5:]`
- 文件产物不入 messages，只写摘要路径
- 任务结束（`graph.end`）时自动判别过程文件并清除：删除工作区内 `_scratch/`，以及本任务写入且非最终交付物的中间文件（规则与前端 `outputFiles.isDeliverableOutputPath` 对齐）；workdir 外路径不碰

### 5.6 确认闸门事件流（替代 v1.1 WS 协议）

LangGraph human-in-the-loop 经 L4 `ConfirmHub`（异步 Future，非原生 `interrupt` 节点）→ SSE `tool.confirm_request` → 渲染进程弹 modal → 用户 POST `/api/tool/confirm/{call_id}` → resolve。审批写入 `{data_dir}/audit.db`。

生产 graph 使用 **AsyncSqliteSaver**（`{data_dir}/checkpoints.db`），`astream` 以 `task_id` 为 `thread_id`。Trace 事件经 TraceBus 同时推 SSE 并落 `{data_dir}/trace.db`（`GET /api/trace/{task_id}` 只读查询）。失败 **replan**（重新拆解）为本方案后续项，当前仅同 subtask retry ≤3。

| SSE event (后端→渲染) | payload | 用途 |
|---|---|---|
| `graph.step` | `{node, step}` | LangGraph 节点进入 |
| `step.delta` | `{delta_text}` | LLM 流式增量 |
| `tool.confirm_request` | `{call_id, tool, args, risk_level}` | human-in-the-loop 请求确认 |
| `tool.result` | `{call_id, tool, result}` | 工具结果回放展示 |
| `llm.fallback` | `{from_index, error}` | 主模型失败切换备用 |
| `artifact.cleanup` | `{paths, count}` | 任务结束清除过程文件（`_scratch` / 中间产物） |
| `graph.end` | `{status: ok\|error\|cancelled, cleaned_paths?, cleaned_count?}` | 任务结束（先清理再结束） |
| `task.done` | `{text, usage}` | 任务完成（兼容） |
| `task.error` | `{code, msg}` | 异常（含 BudgetExhausted） |

| HTTP (渲染→后端) | 用途 |
|---|---|
| `POST /api/tool/confirm/{call_id}` body `{ok: bool}` | 用户点击允许/拒绝 |
| `POST /api/chat` body `{text, attachments}` | 新对话消息 |
| `POST /api/task/{task_id}/cancel` | 用户点停止（真正取消 graph） |
| `POST /api/chat/stop` body `{task_id?}` | 取消指定或全部 RUNNING 任务 |
| `GET /api/trace/{task_id}` | 查询已落库的 trace 事件 |

---

## 6. 工具系统设计

### 6.1 三层工具体系

| 层 | 来源 | 注册方式 | 例 |
|---|---|---|---|
| 内置 Tools | 本项目代码 | hardcode 注册 | `fs.read`, `fs.write`, `exec.bash`, `http.request`, `docx.gen`, `pdf.gen` |
| Skills | 本机 Skills 目录(声明式 yaml+py) | 启动扫描 | `organize_folder`, `weekly_report_from_github`, `lark.send_message` |
| MCP Servers | 社区/自建 | config.toml 声明 `command+args` | `playwright`, `notion`, `filesystem` |

### 6.2 Skills 声明格式

```yaml
# skills/weekly_report_from_github.yaml
name: weekly_report_from_github
description: |
  从 GitHub 拉最近 7 天 PR/Issue，汇总为 DOCX 并保存到用户桌面。
  适用于每周自动周报。
parameters:
  - name: repo
    type: string
    required: true
  - name: since_days
    type: integer
    default: 7
prompt_template: skills/weekly_report_from_github.md   # 注入 LLM 的指令模板
entrypoint: skills/weekly_report_from_github.py        # 可选，纯 Python 子流程
outputs:
  - type: file
    path_pattern: "~/Desktop/reports/{repo}-week-{date}.docx"
allowed_tools:
  - mcp.github.*
  - builtin.docx.gen
  - builtin.fs.write
requires_confirm: true    # 该 Skill 的工具调用默认弹窗确认
```

> 关键约束：`allowed_tools` 是**白名单**（最小权限原则）。Skill 声明的 Worker 只能绑定列出的工具；Worker 执行时若 LLM 调用越界工具，LangGraph tool 节点直接 ToolError。从 OpenHands `agents/skills` 继承的设计。
> Skills 同步：默认各自独立；如团队要共享，把 `skills/` 目录放独立 Git 仓库，各成员 `git pull` 即可，非 MVP 内建同步层。

### 6.3 文档生成内置工具实现要点（v1.2 关键改进）

| 工具 | 库 | 关键点 |
|---|---|---|
| `docx.gen` | python-docx | LLM 生成大纲 JSON → Python 渲染；不让 LLM 直接产 docx XML |
| `xlsx.gen` | openpyxl | 结构化数据先 JSON 校验再填表 |
| `pptx.gen` | python-pptx | 模板驱动：3 套经设计模板，LLM 只选模板+填内容，否则版面灾难 |
| `pdf.gen` | **Electron `webContents.printToPDF`**（**非 weasyprint**） | Python 准备 HTML → IPC 调 Electron 主进程 → Chromium 渲染 → 返回 PDF bytes → Python 落盘。**v1.2 关键改进**：利用 Electron 自带 Chromium，省掉 weasyprint + cairo + pango 整个系统依赖链，跨平台打包零额外成本 |

> PDF 路径选择理由：weasyprint 在 macOS 需 `brew install pango`，Windows 需手动装 GTK runtime，PyInstaller 打包 cairo DLL/dylib 是已知深坑。Electron 已经自带 Chromium（~150MB，反正都要打进去），`printToPDF` API 原生支持 HTML→PDF，质量优于 reportlab，依赖为零。

---

## 7. 执行模型与工作区

### 7.1 执行级别（v1.2：无沙箱档位）

| 档 | 执行位置 | 授权策略 | 适用 |
|---|---|---|---|
| L0 | Python 子进程内 | 静默 | `http.request`（白名单内）、MCP server 调用、`docx/pptx/xlsx.gen`、`lark.send_message` |
| L1 | Python 子进程内 / Electron 主进程 | **弹窗确认** | `fs.read/write`、`exec.bash/powershell`、`pdf.gen` |
| L2（预留） | 无 | — | 未来若做无人值守批处理再引入 Docker/gVisor，本期不实现 |

默认所有 fs/exec 工具走 L1，其他走 L0。Skill 声明可对单个工具设 `requires_confirm: false` 降级为静默（仅限低危如 `os.open_path`），但全局高危命令拒单永远生效不弹窗。

### 7.2 工作区约定（v1.2：本机绝对路径）

- **无统一工作区根**。用户首次启动客户端时配置「允许访问的目录白名单」（默认建议 `~/Desktop`, `~/Documents`, `~/Downloads`），fs 工具校验 path resolve 后必须落在白名单内，越界直接 ToolError。
- 后端类产物（docx/pptx/xlsx）由 Python 直接写到用户指定本机路径（默认 `~/Desktop` 或任务中 LLM 指定的白名单内路径）。
- `pdf.gen` 由 Electron 主进程生成 PDF bytes 后，通过 IPC 回到 Python，Python 落盘到目标路径。
- SQLite 数据库落在 `~/Library/Application Support/my-cowork/db.sqlite`（macOS）或 `%APPDATA%/my-cowork/db.sqlite`（Windows）。
- Skills 配置目录：`~/Library/Application Support/my-cowork/skills/` 或 `%APPDATA%/my-cowork/skills/`。

---

## 8. 入口与远程触发

### 8.1 桌面客户端（核心入口，Electron）

- 对话栏（流式渲染，SSE 接收 `step.delta`）
- 左侧任务/会话列表（持久化在本机 SQLite）
- 右侧 Trace 面板（折叠式步骤 + 工具调用 + token 用量 + React Flow 可视化）
- 顶部模型选择、Skill 库
- 设置页：模型 endpoint + key（写 keychain）、目录白名单、飞书 tunnel 开关、调度任务管理
- **工具调用确认弹窗**：local 工具调用时弹 modal，展示完整命令/路径/参数，用户点「允许/拒绝/总是允许此 Skill 此工具」
- 系统托盘：minimized 状态保持 Python 子进程 + Cloudflare Tunnel 运行，飞书远程任务可唤醒通知

### 8.2 飞书机器人（经 Cloudflare Tunnel）

> **关键约束**：飞书 webhook 触发的任务运行时，桌面客户端必须在运行（托盘 minimized 也算）。app 退出则 webhook 不可达，飞书会收到 502，这类任务失败。这是 v1.2 单机胖客户端的固有约束，明确接受。

路径：
1. 用户在客户端设置页点「启用飞书远程」
2. 客户端调 `cloudflared tunnel --url http://127.0.0.1:<port>` 起一条临时 tunnel，拿到 `https://xxx.trycloudflare.com`
3. 用户在飞书开放平台建自建应用，订阅 `im.message.receive_v1`，回调 URL 填 tunnel URL + `/webhook/lark`
4. 飞书消息 → tunnel → 本机 Python 后端 `/webhook/lark` 端点 → 验签 → 转 TaskRequest 进 TaskManager
5. 任务完成后用 lark-oapi 主动 `send_message` 回原群/原人

安全：webhook 端点验签、IP 白名单（仅飞书出口 IP）、tunnel 仅暴露 `/webhook` 路径不暴露整个端口。
> 注：`trycloudflare.com` 临时 URL 每次重启会变，需用户手动更新飞书回调或绑定固定域名（Cloudflare 账号 + named tunnel，免费）。MVP 接受临时 URL + 手动更新。

### 8.3 定时调度

APScheduler + SQLite jobstore（本机文件），支持 cron / interval / date 三种触发。Skill 可声明 `schedule: "0 9 * * 1"`（每周一 9 点）自动注册。
> **约束**：定时任务只在客户端运行时触发。app 关机则不跑，下次启动 APScheduler 会补跑 misfire（可配置 `misfire_grace_time`）。明确接受此约束——若需 24h 可靠调度，需回到 v1.1 C/S 架构租用机。

---

## 9. 模型层（成本控制关键）

### 9.1 多模型路由策略

| 任务类型 | 默认模型 | 备选 | 理由 |
|---|---|---|---|
| Supervisor/Workers 主推理 | Claude Sonnet 级 / GLM-4.5 | GPT-4o | 工具调用稳定性最高 |
| 上下文 summary 压缩 | GLM-4-flash / gpt-4o-mini | Qwen-turbo | 便宜，质量足够 |
| 文档大纲生成 | 同主模型 | — | 一致性 |
| 简单分类/意图识别（飞书入参） | GLM-4-flash | — | 量大事小 |

路由由 `llm_gateway` 内 `model_picker(task_kind)` 决定，可在客户端设置页覆盖。

### 9.2 成本闸

- Budget 对象（见 §5.2）：max_total_tokens=200k/任务，硬截断
- 全局 daily token 上限（写 SQLite `usage_log`，每日聚合对比阈值），超限客户端弹告警 + 飞书通知
- Trace 面板实时显示「本任务已花 $X」

---

## 10. 记忆系统

**只用 1 张 SQLite + sqlite-vec 表**，不引入完整记忆框架：

```sql
CREATE TABLE memory (
  id INTEGER PRIMARY KEY,
  task_id INTEGER,
  kind TEXT,        -- 'preference'|'fact'|'correction'
  content TEXT,
  embedding BLOB,   -- 由 llm_gateway.embedding() 生成
  created_at TIMESTAMP,
  expires_at TIMESTAMP   -- 默认 30 天
);
```

写入时机：用户明确说「记住：我周报要发到 #dev 频道」「以后 PDF 用模板B」→ Agent 识别为 correction/preference → 调 `memory.write`。
查询时机：每任务启动注入 top-3 相关记忆到 system prompt。

> 不做全量对话回灌向量库（成本高、噪声大、单机自用无价值）。各成员记忆库独立，不共享。

---

## 11. 安全清单（v1.2：单机胖客户端）

**本机文件/Shell 操作（主要风险面）**
- [ ] 所有 local 工具调用必须弹窗确认，展示完整命令/路径/参数；用户可选「本次允许/总是允许此 Skill 此工具/拒绝」
- [ ] 高危命令硬拒单（不进弹窗直接拒）：`rm -rf /`、`rm -rf ~`、`mkfs`、`dd if=/dev/zero of=/dev/sd*`、`chmod -R 777 /` 等模式匹配
- [ ] fs 工具路径白名单：客户端启动时配置允许访问目录，fs 调用前校验 path resolve 后落在白名单内
- [ ] exec 工具的 `cwd` 强制限定在白名单目录内
- [ ] 建议用户开启 macOS Time Machine / Windows 文件历史，作为最后一道文件安全网

**API key 管理**
- [ ] key 存 keychain（keytar），不落盘配置文件
- [ ] Python 子进程启动时由 Electron 主进程注入 env，子进程内不打印 key 到日志
- [ ] 结构化日志 redact `Authorization`/`api-key` 字段

**网络与 webhook**
- [ ] Cloudflare Tunnel 仅暴露 `/webhook/lark` 和 `/webhook/slack` 路径，不暴露整个后端端口
- [ ] 飞书 webhook 验签 + IP 白名单（仅飞书出口 IP）
- [ ] `http.request` 工具出站网络白名单（默认仅允许 LLM endpoint + Skill 显式声明域；可在设置页配置）
- [ ] MCP server 启动命令白名单（`config.toml` 显式列出可执行 path，禁止 `/bin/sh -c <llm-string>`）

**进程与打包**
- [ ] Python 子进程 bind `127.0.0.1` 仅本机，不 bind `0.0.0.0`（除 tunnel 路径）
- [ ] `skills/` 目录加载时校验签名（GPG；自用 PSK 签），防供应链投毒
- [ ] electron-updater 校验签名（自用 PSK），防中间人替换
- [ ] 每日 token 上限告警

**架构层**
- [ ] 确认闸门等待超时 600s，超时后 task 标记 failed 不阻塞 Loop 协程
- [ ] 飞书 webhook 触发的任务若 Skill 含 local 工具，仍走弹窗确认（用户不在屏幕前则 600s 超时失败，可接受）
- [ ] 上下文每任务新建 AgentContext，绝不复用 messages

---

## 12. 项目目录结构（v1.4：单仓库，Electron + Python 后端严格 9 层 harness 分层 + LangGraph）

> 后端 `backend/app/` 下 9 层分层对齐 §4.6，其中 L7 执行层扩展为三个子包：`graphs/`（LangGraph StateGraph 定义）、`agents/`（Agent 工厂与 prompt）、`runtime/`（graph 执行、checkpoint、预算、压缩）。跨层依赖只允许向下（CI 用 import-linter 校验，见 §12.1）。

```
my-cowork/
├── package.json                        # electron + vite + react + ts 顶层
│
├── electron/                           # ===== Electron 主进程 (Node.js) =====
│   ├── main.ts                         # 主进程入口, spawn Python 子进程
│   ├── preload.ts                      # contextBridge IPC
│   ├── python_runner.ts                # spawn PyInstaller 可执行 + 健康检查 + 端口分配
│   ├── keychain.ts                     # keytar 封装, 读 key 注入 Python env (L9 部分)
│   ├── ipc_handlers.ts                 # pdf.gen / os.open_path / 文件对话框 (跨 L3-Electron)
│   ├── tunnel.ts                       # cloudflared 子进程管理 (L9 入口网关)
│   └── updater.ts                      # electron-updater
│
├── src/                                # ===== Electron 渲染进程 (React + Vite + TS) =====
│   ├── App.tsx
│   ├── components/
│   │   ├── chat/                       # 对话栏 + SSE 流
│   │   ├── trace/                      # React Flow trace 可视化 (消费 L5 事件)
│   │   ├── skills/                     # Skill 库浏览
│   │   ├── confirm_modal/              # 工具授权弹窗 (消费 L4 confirm_request 事件)
│   │   └── settings/                   # key/白名单/tunnel/调度
│   ├── store/                          # zustand
│   └── api/                            # SSE client + HTTP 封装
│
├── backend/                            # ===== Python 后端子工程 (PyInstaller 打包目标) =====
│   ├── pyproject.toml                  # uv 管理, 含 import-linter 分层契约配置
│   ├── app/
│   │   ├── main.py                     # FastAPI app 装配 (组合各层, 不含业务逻辑)
│   │   │
│   │   ├── server/                     # ── L9 入口层 ──
│   │   │   ├── routes/
│   │   │   │   ├── chat.py             # SSE 流 (渲染进程用)
│   │   │   │   ├── confirm.py          # POST /api/tool/confirm/ (L4 审批门回填)
│   │   │   │   ├── webhook_lark.py     # 飞书 webhook
│   │   │   │   ├── webhook_slack.py
│   │   │   │   └── admin.py            # 设置/调度任务管理
│   │   │   ├── scheduler.py            # APScheduler 封装 (本机定时)
│   │   │   └── __init__.py             # 暴露 create_app()
│   │   │
│   │   ├── orchestrator/               # ── L8 编排层 ──
│   │   │   ├── task_manager.py         # 任务生命周期/会话状态 (启动 graph、取消、状态查询)
│   │   │   ├── task_handle.py          # TaskHandle 数据类
│   │   │   └── __init__.py             # 暴露 submit_task()/cancel_task()
│   │   │
│   │   ├── graphs/                     # ── L7a LangGraph StateGraph ──
│   │   │   ├── supervisor.py           # Supervisor/Workers 状态图定义
│   │   │   ├── routing.py             # 节点间条件边 / handoff 路由
│   │   │   ├── state.py               # LangGraph State TypedDict
│   │   │   └── __init__.py            # 暴露 compile_supervisor_graph()
│   │   │
│   │   ├── agents/                     # ── L7b Agent 工厂与 prompt ──
│   │   │   ├── factory.py             # create_supervisor / create_worker
│   │   │   ├── prompts/               # 各 Agent system prompt
│   │   │   │   ├── supervisor.md
│   │   │   │   ├── doc_worker.md
│   │   │   │   ├── file_worker.md
│   │   │   │   ├── web_worker.md
│   │   │   │   └── msg_worker.md
│   │   │   └── __init__.py            # 暴露 agents 实例
│   │   │
│   │   ├── runtime/                    # ── L7c graph 执行与预算 ──
│   │   │   ├── graph_runner.py        # 启动/流式消费 graph event
│   │   │   ├── checkpointer.py        # SQLite-backed LangGraph checkpointer
│   │   │   ├── budget.py              # 步数/token 预算闸
│   │   │   ├── compressor.py          # 上下文压缩 (调 L2 廉价模型)
│   │   │   └── __init__.py            # 暴露 run_graph()
│   │   │
│   │   ├── memory/                     # ── L6 记忆层 ──
│   │   │   ├── short_term.py          # 会话上下文存取
│   │   │   ├── long_term.py           # sqlite-vec 向量记忆
│   │   │   └── __init__.py            # 暴露 read_memory()/write_memory()
│   │   │
│   │   ├── observability/              # ── L5 可观测层 (横切, 各层都向它发事件) ──
│   │   │   ├── trace.py               # trace 事件 (经 SSE 推渲染进程)
│   │   │   ├── metrics.py             # token 计数/成本统计
│   │   │   ├── logger.py              # structlog 结构化日志 (redact key)
│   │   │   ├── replay.py              # 历史 trace 回放
│   │   │   └── __init__.py            # 暴露 emit()/subscribe()
│   │   │
│   │   ├── guardrails/                 # ── L4 护栏层 (工具调用前后钩子) ──
│   │   │   ├── policy.py              # 策略引擎 (Skill 工具白名单校验)
│   │   │   ├── approval.py            # 审批门 (confirm_hub: await future, SSE 推确认请求)
│   │   │   ├── command_filter.py      # 高危命令硬拒单 (模式匹配)
│   │   │   ├── audit.py               # 审计日志 (写 SQLite)
│   │   │   └── __init__.py            # 暴露 pre_tool_hook()/post_tool_hook()
│   │   │
│   │   ├── tools/                      # ── L3 工具层 ──
│   │   │   ├── registry.py            # 注册中心 + schema 暴露给 LLM
│   │   │   ├── builtin/
│   │   │   │   ├── fs.py              # fs.read/write/list (调 L1 path_guard)
│   │   │   │   ├── exec.py            # exec.bash/powershell (调 L1 + L4 command_filter)
│   │   │   │   ├── http.py            # http.request (调 L1 net_guard)
│   │   │   │   ├── os_tools.py       # os.open_path (调 Electron IPC)
│   │   │   │   └── docgen/
│   │   │   │       ├── docx_gen.py
│   │   │   │       ├── pptx_gen.py
│   │   │   │       ├── xlsx_gen.py
│   │   │   │       └── pdf_gen.py     # 调 Electron IPC printToPDF (非 weasyprint)
│   │   │   ├── mcp/
│   │   │   │   └── manager.py         # MCP client stdio/sse
│   │   │   ├── skills/
│   │   │   │   └── loader.py          # 扫描 skills/ 目录, 加载 yaml+py
│   │   │   ├── lark/
│   │   │   │   └── send_message.py    # lark-oapi 封装
│   │   │   └── __init__.py            # 暴露 register()/invoke()/schemas()
│   │   │
│   │   ├── llm/                        # ── L2 模型层 ──
│   │   │   ├── gateway.py             # 统一抽象: messages in → stream out
│   │   │   ├── providers/
│   │   │   │   ├── anthropic.py       # Anthropic Messages API
│   │   │   │   └── openai_compat.py   # OpenAI v1 / OpenRouter
│   │   │   ├── router.py              # model_picker (主模型/压缩模型/分类)
│   │   │   ├── fallback.py            # fallback 链 (Anthropic→OpenRouter→GLM)
│   │   │   ├── token_counter.py       # tiktoken 估算
│   │   │   └── __init__.py            # 暴露 complete()/embed()
│   │   │
│   │   ├── sandbox/                    # ── L1 沙箱层 (执行约束, 策略独立) ──
│   │   │   ├── path_guard.py          # 路径白名单校验 (fs/exec 共用)
│   │   │   ├── net_guard.py           # 出网白名单 (http 共用)
│   │   │   ├── policy.py              # L0/L1 级别策略 + Skill requires_confirm 解析
│   │   │   └── __init__.py            # 暴露 check_path()/check_net()/get_policy()
│   │   │
│   │   └── config.py                  # pydantic-settings 读 env (key 由 Electron 注入, 不落盘)
│   │
│   ├── skills/                         # Skill 包目录 (随包打包, 可选 Git 同步)
│   │   └── weekly_report_from_github/
│   │       ├── skill.yaml
│   │       ├── prompt.md
│   │       └── entry.py
│   ├── config.template.toml
│   ├── tests/
│   │   ├── test_layering.py           # import-linter 分层契约测试 (CI 强制)
│   │   └── ...
│   └── .importlinter                  # import-linter 配置 (声明 9 层 contract)
│
├── build/                              # 打包配置
│   ├── pyinstaller/
│   │   ├── macos.spec                  # PyInstaller spec, --onefile, hiddenimports 列表
│   │   └── windows.spec
│   └── electron-builder.yml            # .dmg / .exe 配置
│
├── scripts/
│   ├── build-python-macos.sh           # CI: uv sync + pyinstaller + 烟雾测试
│   ├── build-python-windows.ps1
│   └── build-electron.sh               # CI: npm run build + electron-builder
│
└── docs/
    ├── 部署手册.md                      # 用户装 .dmg/.exe 指南
    └── 开发指南.md                      # 本地 dev (分跑 Python + Vite) + import-linter 校验说明
```

### 12.1 import-linter 分层契约（CI 强制）

`backend/.importlinter` 声明 harness 分层 contract，违反分层直接 build 失败：

```ini
[importlinter]
root_package = app

[importlinter:contract:harness-layers]
name = Harness layered dependency direction
type = layers
layers =
    app.server
    app.orchestrator
    app.graphs
    app.runtime
    app.agents
    app.memory
    app.observability
    app.guardrails
    app.tools
    app.llm
    app.sandbox
```

> L5 observability 与 L4 guardrails 是横切层，用独立 contract 放宽其被任意层引用；但它们自身不得反向依赖业务层（server/orchestrator/graphs/runtime/agents），由 `forbidden` contract 单独约束。
> L1 sandbox 与 L2 llm 也不得反向依赖业务层，由独立 `forbidden` contract 约束。
> `main.py` 是唯一允许组合多层的入口（装配各层依赖注入），不参与分层约束。

---

## 13. 里程碑与验收标准（v1.4：以可演示为硬指标）

### M1 - Python 后端 LangGraph 多 Agent 通跑（第 1-2 周）
**验收**：本机 `uv run app.main`，`curl POST /api/chat` 发「在 ~/Desktop 下写一个 hello.txt 内容为 hi」任务，Supervisor 路由到 FileWorker，调用 `fs.write`，文件真的出现在桌面，完整 trace JSON 写入 SQLite，task 走到 done。
**不包含**：Electron 壳、UI、确认弹窗、MCP、文档生成。
**开发模式**：Python + Vite 分跑，不打 PyInstaller 包。

### M2 - Electron 壳 + 渲染进程 UI + 确认闸门（第 3-4 周）
**验收**：用户启动 Electron app，看到 React UI，输入「在 ~/Desktop 下写 hello.txt」→ 后端触发 `fs.write` → 渲染进程弹确认 modal → 用户点允许 → 文件落盘 → trace 实时流式显示。Electron 主进程 spawn Python 子进程跑通，端口动态分配 + 健康检查。
**包含**：Electron 主进程 + preload + `python_runner.ts`、React 基础 UI、SSE 流、`confirm_hub` + `confirm_modal`、路径白名单配置、高危命令拒单。仍未打 PyInstaller 包，开发模式跑。

### M3 - 文档生成 + MCP（第 5-6 周）
**验收**：客户端输入「生成 PPT《2026Q1 总结》5 页」，`pptx.gen` 生成落到桌面；输入「把这份 HTML 转 PDF」→ `pdf.gen` 调 Electron `printToPDF` → PDF 落到桌面；接入 playwright MCP 完成「打开 example.com 截图」任务，截图落盘。
**包含**：docx/pptx/xlsx 三个内置工具、PDF 走 Electron IPC、MCP stdio client、至少 2 个 MCP server 接入示例（playwright + filesystem）。

### M4 - 飞书远程 + 定时调度（第 7-8 周）
**验收**：客户端设置页点「启用飞书远程」→ `cloudflared` 起来 → 用户在飞书填回调 URL → 飞书发消息「生成测试 docx 落到桌面」→ 客户端弹确认（用户在屏幕前点允许）→ 任务跑完结果回飞书；同时配一个 cron skill 每周一 9 点自动跑周报（客户端运行时才触发）。
**包含**：`tunnel.ts` cloudflared 管理、lark-oapi 集成、webhook 验签、APScheduler、SQLite jobstore。

### M5 - 记忆 + 成本闸 + 打包分发（第 9-11 周）
**验收**：用户说「以后 PDF 都用模板B」，下次任务 system prompt 注入该偏好；超 200k token 任务被硬截断并报错；**PyInstaller + electron-builder 打出 `.dmg`（macOS）和 `.exe`（Windows）**，干净机器（无 Python 无 Node）双击安装能跑通 M1 验收用例；keychain 读写 key 跑通；写部署手册。
**包含**：sqlite-vec 记忆注入、Budget 硬约束、PyInstaller spec、electron-builder 配置、GitHub Actions 矩阵 build、`docs/` 部署手册。**这是 v1.2 最大风险里程碑**（见 §15）。

> 全程 11 周，按 1 人全职或 2 人兼职估算。无 Computer Use、SSO、多租户、共享后端等范围外项。多智能体采用 LangGraph Supervisor/Workers 模式。

---

## 14. 成本估算（v1.2：单机胖客户端，无租用机）

### 14.1 基础设施
- 后端服务器：**0**（无租用机）
- 公网暴露：**0**（Cloudflare Tunnel 免费版；如需固定域名用自有域名 + Cloudflare 免费 named tunnel）
- 域名 + SSL：0（用 `*.trycloudflare.com` 临时 URL；或自有域名 + Let's Encrypt 0 元）

### 14.2 模型 token（各成员各自承担自己的 key 费用）
假设：日均 20 个办公任务，平均每任务 8 步，每步平均 3k in / 800 out tokens。

- 主模型用 Claude Sonnet：$3/$15 per M tokens
  - 月用量：20*30*8 = 4800 步 * 3.8k = 18.24M tokens（约 14:3 输入输出比例）
  - ≈ $14*3/1000 + $3*15/1000 * 18.24 ≈ $54.7 + $8.2 ≈ **月 $63 / 人**
- 压缩 + 记忆用 GLM-4-flash（$0.1/$0.1 per M）：忽略不计
- 切 GLM-4.5（$0.5/$2 per M）：约 **$15/月 / 人**

### 14.3 合计
- Claude 主模型路线：**$63/月/人**，无基础设施费
- GLM 主模型路线：**$15/月/人**，无基础设施费
- 团队 N 人 = N 倍（各自承担自己的 key，无共享成本）

> 对比 v1.1：v1.1 是 ¥650/月全团队共享；v1.2 是 $63*N/月各人独立。3 人团队 v1.2 ≈ $189 ≈ ¥1350，比 v1.1 贵约 2 倍，但省了租用机运维 + 公网暴露面 + WS 反调工程风险。

---

## 15. 主要风险与对策（v1.2）

| 风险 | 概率 | 影响 | 对策 |
|---|---|---|---|
| **PyInstaller 打包漏 hiddenimports，运行时 ImportError** | **高** | **打包后不能用** | **CI 矩阵跑 macOS/Windows 烟雾测试，M5 在干净机器验证；列出已知 hiddenimports：python-docx/openpyxl/python-pptx/lark-oapi/sqlite-vec** |
| **PyInstaller 包过大（>200MB）** | 中 | 分发慢 | 接受；排除测试依赖、用 `--onefile` 压缩；Playwright 浏览器按需下载不打入包 |
| Electron spawn Python 子进程启动慢 | 中 | 首次体验差 | `--onefile` 解压到缓存目录复用；启动 splash 屏遮盖；健康检查轮询 |
| LLM 生成 shell 越权删除本机文件 | 中 | 数据丢失 | 高危命令硬拒单 + 每条 exec 弹窗确认 + 路径白名单 + 建议开启 Time Machine |
| 确认闸门用户离开卡 600s | 中 | 任务 hang | 600s 超时后 task failed；可在设置页调超时；飞书触发任务依赖用户在屏幕前 |
| 飞书 webhook 时客户端未运行 | 高 | 任务失败 | tunnel 不可达飞书收到 502；客户端启动时主动检查并提示；接受此约束 |
| cloudflared 临时 URL 重启变更 | 中 | 需手动更新飞书回调 | MVP 接受；v1.3 可用 Cloudflare named tunnel + 自有域名固定 URL |
| 定时任务 app 关机不跑 | 高 | 任务漏跑 | APScheduler misfire 补跑；接受约束；若需 24h 可靠需回 v1.1 架构 |
| MCP server 供应链投毒 | 低 | RCE | GPG 签名校验 + 启动命令白名单 |
| Anthropic API 偶发限流 | 中 | 任务失败 | llm_gateway fallback 链：Anthropic → OpenRouter → GLM |
| 上下文污染（前任务残留） | 中 | 错误执行 | 每任务新建 AgentContext，绝不复用 messages |
| 办公文档版面丑 | 高 | 体验差 | 模板驱动：docx/pptx 提供经设计模板，LLM 只填字段不控版式 |
| 客户端被恶意 Skill 诱导授权 | 中 | 本机数据泄漏 | Skill GPG 签名校验；高危命令硬拒单永远生效不弹窗；「总是允许」仅对单 Skill 单工具生效且可一键回收 |

---

## 16. 与竞品的差异化结论（v1.2）

> **走 OpenWork 已验证的「单机胖客户端」路线，但用 Python 后端 + LangGraph 多 Agent 编排替代其 TypeScript + Claude Agent SDK，换取办公文档生成生态（python-docx/openpyxl/python-pptx）、模型无关性与可扩展的多 Agent 能力；舍弃 OpenCowork 的 WSL2/Lima VM 隔离换为「授权弹窗 + 路径白名单 + 高危拒单」的更轻量形态；用 Cloudflare Tunnel 解决飞书 webhook 公网回调，零租用机成本；用 Electron 自带 Chromium 做 PDF 生成，绕开 weasyprint+cairo 打包深坑。最终拿到 80% 办公自动化价值、20% 工程量、零基础设施成本，代价是失去 24h 在线和打包工程风险。**

---

## 17. 会话预览：Office 渲染与轻量编辑

Session Preview 对 Word / Excel 的渲染与编辑职责如下（与 Agent 沙箱写文件分离：预览写回是用户在面板主动保存）。

### 能力矩阵

| 类型 | 能力 | 技术 |
|------|------|------|
| `.docx` | 高保真只读预览 | `docx-preview`（渲染端 `readFileBuffer` → `renderAsync`） |
| `.xlsx` / `.csv` | 预览 + 单元格值轻量编辑 + 写回 | SheetJS Community `xlsx@0.18.5` |
| `.doc` | 简化 HTML 预览 | mammoth（建议另存为 docx） |
| `.pptx` | 文本级 HTML 预览 | 现有 unzip + XML |

**本期不做**：Word 正文编辑；Excel 公式重算、图表、合并单元格 UI、透视表、宏；OnlyOffice / WPS WebOffice。

### 数据流

1. `FilePreview` 对 `docx` / `xlsx` / `csv` 不再走主进程 `open-file`→HTML。
2. Electron IPC：`read-file-buffer`（上限 80MB）、`write-file-buffer`、`dialog:save-file`。
3. Excel 编辑只改显示值；原公式格被编辑后写回为**常量**（清除 formula），不重算依赖。
4. 单元格数 &gt; 30_000：只读截断预览（前 500 行），禁止应用内保存覆盖。
5. 未保存切换 / 关闭 Preview tab 时 `confirm` 拦截。

### 与生成链路的边界

- Agent 侧文档生成仍用 Python（`python-docx` / `openpyxl` / `python-pptx`）写工作区。
- Preview 侧写回不走后端 PathGuard，属于本机「用户打开文件 → 主动保存」路径。

---

## 附录 A：调研来源（可复核）

- OpenHands: https://github.com/OpenHands/OpenHands （82.3k★, Python, Docker/VM/云后端）
- Eigent: https://github.com/eigent-ai/eigent （14.6k★, FastAPI+CAMEL, local-first / cloud 双模式）
- OpenCowork: https://github.com/OpenCoworkAI/open-cowork （1.9k★, Electron+TS, WSL2/Lima, 飞书/Slack）
- OpenWork: https://github.com/Xerus-ai/openwork （34★, Claude Agent SDK, OpenRouter, **单机胖客户端**）
- openwork-hub (技能/agent 资产): https://github.com/different-ai/openwork-hub
- CAMEL-AI 框架: https://github.com/camel-ai/camel （Eigent 后端）
- MCP 协议规范: https://modelcontextprotocol.io
- Anthropic Messages API 流式协议: https://docs.anthropic.com/en/api/messages-streaming
- Cloudflare Tunnel: https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/
- PyInstaller: https://pyinstaller.org
- electron-builder: https://www.electron.build
- keytar (keychain 跨平台封装): https://github.com/atom/node-keytar
- deer-flow (super agent harness): https://github.com/bytedance/deer-flow （77.9k★, sub-agents+memory+sandboxes+skills）
- water (production-ready agent harness framework): https://github.com/manthanguptaa/water （harness 定义最清晰：agent 周围基础设施, 非 agent 本身）
- omnigent (meta-harness): https://github.com/omnigent-ai/omnigent （7.8k★, 编排多第三方 agent）
- audit-harness (审计执行保障): https://github.com/RickyTong1/audit-harness （577★, 三层防线+审计日报）
- import-linter (分层契约 CI 校验): https://import-linter.readthedocs.io

## 附录 B：与各竞品的能力差异核对表（v1.3 自查）

| 能力 | 我们方案 v1.2 | OpenHands | Eigent | OpenCowork | OpenWork |
|---|---|---|---|---|---|
| 模型无关 | ✓ 自研 gateway | ✓ | ✓ | ✓ | ✗ Claude SDK |
| 强隔离沙箱 | ✗ 授权闸门 | ✓ Docker/VM | 弱 | ✓ WSL2/Lima | ✗ 文件夹约束 |
| 本机磁盘直访 | ✓ 进程内 | ✗ | ? | ✗（VM 内） | ✓ |
| 单机胖客户端 | ✓ Electron+Python | ✗ web | ✗ C/S | ✗ C/S（VM） | ✓ Electron+TS |
| 零基础设施成本 | ✓ 无租用机 | ✗ 需服务器 | ✗ 需服务器 | ✓ | ✓ |
| 飞书远程 | ✓ Cloudflare Tunnel | ✗ | ? | ✓ | ✗ |
| 定时调度 | ✓ 本机 APScheduler | ✓ Automation | ✓ | ? | ✗ |
| 24h 在线 | ✗ 关机不跑 | ✓ | ✓ | ✓ | ✗ |
| 文档生成内置 | ✓ 4 类 | 外部 | ✓ | ✓ | ✓ |
| PDF 生成方案 | ✓ Electron Chromium | 外部 | ? | ? | ? |
| Computer Use | ✗ 不做 | 外部 | ✓ Gemini | ✓ Gemini | ✗ |
| 多智能体编排 | ✓ LangGraph Supervisor/Workers | 单为主 | ✓ Workforce | 单 | 单 |
| MCP 接入 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 记忆向量化 | ✓ sqlite-vec | ? | CAMEL | ? | ✗ |
| 成本闸(硬截断) | ✓ Budget (L7 runtime) | ? | ? | ? | ✗ |
| 跨平台桌面壳 | ✓ Electron | ✗ web | ✓ Electron | ✓ Electron | ✓ Electron |
| **harness 9 层分层** | ✓ CI import-linter 强制 | ? 部分 | ? 部分 | ✗ | ✗ |
| API key 集中管理 | ✗ 各自 keychain | ✓ 后端 | ? | ? | ? |

---

文档版本：v1.4 / 变更要点：LangGraph + Supervisor/Workers 多 Agent 编排、单机胖客户端（Electron 内嵌 Python 后端）、无租用机、Cloudflare Tunnel 暴露 webhook、PDF 走 Electron printToPDF、key 存 keychain、各成员独立部署、本机 APScheduler（关机不跑）/ 评审前先用